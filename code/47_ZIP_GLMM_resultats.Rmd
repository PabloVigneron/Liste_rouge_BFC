---
title: "LRR des poissons d'eau douce de BFC"
subtitle: "47_ZIP_GLMM_resultats"
author: "PabloVigneron"
date: "`r format(Sys.time(), '%Y-%m-%d')`"
output: 
  bookdown::html_document2:
   fig_caption: yes
   tab_caption: yes
   number_sections: yes
   global_numbering: yes
   toc: yes
   toc_float:
         toc_collapsed: yes
         toc_depth: 2
   code_folding: hide
   css: "style.css"
#bibliography: .bib
#link-citations: true
#cls: apa.cls
#editor_options: 
#  markdown: 
#    wrap: sentence
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE,
                      message = FALSE,
                      warning = FALSE)
```

Script : 47_ZIP_GLMM_resultats

  Ce script a pour objectif d'évaluer l'impact des différents réseaux sur les tendances des indicateurs régionaux. Il a également l'objectif de regarder l'impact des changements de protocoles sur les stations au cours des années d'études grâce aux zero inflated poisson GLMM. 


# Installation 

## Chargement des packages, fonctions et des données 

```{r Chargement des packages}
library(lme4)
library(aspe)
library(tidyverse)
library(aspe)
library(sjstats)
library(gt)
library(glmmTMB)
library(RVAideMemoire)
library(lmerTest)  # remplace lme4 automatiquement
library(ggh4x)

```


```{r Chargement des données}
load(file = "../processed_data/selection_especes.rda")
load(file = "../processed_data/selection_pop_ope.rda")
load(file = "../processed_data/donnees_densite_eff_abs.rda")
load(file = "../processed_data/assemblage_tab_par_ope.rda")
```

```{r Chargement des fonctions}
# source(file = "../R/lm_application_modele.R")
# source(file = "../R/lm_calcul_modele.R")
source(file = "../R/zip_glmm_calcul_modele.R")
source(file = "../R/zip_glmm_application_modele.R")
source(file = "../R/glmm_calcul_modele.R")
source(file = "../R/glmm_application_modele.R")
source(file = "../R/adequate_data_for_model.R")

```

```{r Chargement des tables de données}
rdata_tables <- misc_nom_dernier_fichier(
  repertoire = "../../../../Liste_rouge_BFC/raw_data",
  pattern = "^tables")
load(rdata_tables) # Chargement des données 
```



# Modèles Linéaires Généralisés à effets Mixtes (GLMM)

## Introduction

Les modèles linéaires généralisés à effets mixtes combinent les caractéristiques des modèles linéaires généralisés (modéliser des variables non-normalement distribuées, spécialement des données binaires et de comptage) et des modèles à effets mixtes (modéliser des données groupées). 

Nous utiliserons dans cette étude le package *glmmTMB* pour ajuster des modèles mixtes. La fonction *glmmTMB* de ce package estime les paramètres d’un modèle linéaire généralisé à effets mixtes. Les formules utilisées par *glmmTMB* suivent la forme *reponse ~ predicteurs*, avec une syntaxe spécifique pour les effets aléatoires.

La distribution de Poisson peut être ici utilisée (valeurs entières supérieures ou égales à 0.  glmmTMB(y ~ x1 + x2 + ..., data = ..., family = poisson(link=log))

## Pré-traitements GLMM Biomasse
```{r Etablir les périodes etudes GLMM}
liste_periodes_etude <- list(c(2007, 2025), 
                             c(2015, 2025)) # Liste des périodes (chaque période est un vecteur avec l'année de début et l'année de fin) 
```


```{r preparation du df pour zip glmm biomasse}
# Ajout des coordonnées pour jointure avec df pour future analyse autocorrelation spatiale
coord_sta_id <- point_prelevement %>%
  filter(pop_id %in% pop_serie_tempo) %>%
  rename(x = pop_coordonnees_x, y = pop_coordonnees_y) %>%
  select(pop_id, x, y)

# preparer la table pour jointure
ope_selection <- ope_selection %>%
  dplyr::select(ope_id, pop_id, sta_id)

ope_biomasse_glm <- ope_indicateur %>%
  mef_ajouter_type_protocole() %>%
  mef_ajouter_ope_date() %>% 
  mef_ajouter_surf_calc() %>% 
  # mef_ajouter_objectif() %>%  # Ajout  des objectifs de pêches
  inner_join(y = ope_selection, by = c("ope_id", "annee", "pop_id")) %>% 
  left_join(coord_sta_id, by = "pop_id") %>%
  mutate(
    valeur = as.numeric(valeur) ,
    annee = as.numeric(annee),
    # annee_an = annee,
    ope_surface_calculee = as.numeric(ope_surface_calculee) ,
    pro_libelle = as.factor(pro_libelle),
    pop_id = as.factor(pop_id),
    sta_id = as.factor(sta_id),
    # obj_libelle = as.factor(obj_libelle),
    # indicateur = "effectif_total"
    ) %>% 
  # ) %>% # creation d'une colonne pour la période (ex : 2007-2008)
  # mutate(annee = {
  #   debut <- floor((annee - annee_debut_periode) / 2) * 2 + annee_debut_periode
  #   paste0(debut)
  # }, annee = as.numeric(annee)) %>% 
  # mutate(julian = lubridate::yday(ope_date)) %>% #  Ajout de la date de pêche codée de 1 à 365
  select(
    ope_id,
    valeur,
    esp_code_alternatif,
    annee,
    ope_surface_calculee,
    pro_libelle,
    pop_id,
    sta_id,
    indicateur,
    x,
    y,
    ope_date,
    # julian, 
    # obj_libelle
  ) %>% 
  rename(espece = esp_code_alternatif) %>% 
  filter(indicateur == "biomasse")
```

```{r}
vai <- ope_biomasse_glm %>% 
  filter(espece == "VAI")
summary(vai)
```

## Verification des conditions d'application du modele 

```{r}

abl_data <- ope_biomasse_glm %>%
  filter(espece == "ABL")
mod_1 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                              sta_id), data = abl_data)
ang_data <- ope_biomasse_glm %>%
  filter(espece == "ANG")
mod_2 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                              sta_id), data = ang_data)
baf_data <- ope_biomasse_glm %>%
  filter(espece == "BAF")
mod_3 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                              sta_id), data = baf_data)
bln_data <- ope_biomasse_glm %>%
  filter(espece == "BLN")
mod_4 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                              sta_id), data = bln_data)

brb_data <- ope_biomasse_glm %>%
  filter(espece == "BRB")
mod_5 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                              sta_id), data = brb_data)

bre_data <- ope_biomasse_glm %>%
  filter(espece == "BRE")
mod_6 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                              sta_id), data = bre_data)

bro_data <- ope_biomasse_glm %>%
  filter(espece == "BRO")
mod_7 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                              sta_id), data = bro_data)

cco_data <- ope_biomasse_glm %>%
  filter(espece == "CCO")
mod_8 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                              sta_id), data = cco_data)

cha_data <- ope_biomasse_glm %>%
  filter(espece == "CHA")
mod_9 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                              sta_id), data = cha_data)

che_data <- ope_biomasse_glm %>%
  filter(espece == "CHE")
mod_10 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = che_data)

epi_data <- ope_biomasse_glm %>%
  filter(espece == "EPI")
mod_11 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = epi_data)


ept_data <- ope_biomasse_glm %>%
  filter(espece == "EPT")
mod_12 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = ept_data)


gar_data <- ope_biomasse_glm %>%
  filter(espece == "GAR")
mod_13 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = gar_data)


gou_data <- ope_biomasse_glm %>%
  filter(espece == "GOU")
mod_14 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = gou_data)


hot_data <- ope_biomasse_glm %>%
  filter(espece == "HOT")
mod_15 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = hot_data)


lof_data <- ope_biomasse_glm %>%
  filter(espece == "LOF")
mod_16 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = lof_data)


lor_data <- ope_biomasse_glm %>%
  filter(espece == "LOR")
mod_17 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = lor_data)


lot_data <- ope_biomasse_glm %>%
  filter(espece == "LOT")
mod_18 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = lot_data)


lpp_data <- ope_biomasse_glm %>%
  filter(espece == "LPP")
mod_19 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = lpp_data)


obr_data <- ope_biomasse_glm %>%
  filter(espece == "OBR")
mod_20 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = obr_data)


per_data <- ope_biomasse_glm %>%
  filter(espece == "PER")
mod_21 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = per_data)


rot_data <- ope_biomasse_glm %>%
  filter(espece == "ROT")
mod_22 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = rot_data)


sat_data <- ope_biomasse_glm %>%
  filter(espece == "SAT")
mod_23 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = sat_data)



spi_data <- ope_biomasse_glm %>%
  filter(espece == "SPI")
mod_24 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = spi_data)


tan_data <- ope_biomasse_glm %>%
  filter(espece == "TAN")
mod_25 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = tan_data)


tox_data <- ope_biomasse_glm %>%
  filter(espece == "TOX")
mod_26 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = tox_data)


trf_data <- ope_biomasse_glm %>%
  filter(espece == "TRF")
mod_27 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = trf_data)


vai_data <- ope_biomasse_glm %>%
  filter(espece == "VAI")
mod_28 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = vai_data)


van_data <- ope_biomasse_glm %>%
  filter(espece == "VAN")
mod_29 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = van_data)

```

```{r}
plotresid(mod_1)
plotresid(mod_2)
plotresid(mod_3)
plotresid(mod_4)
plotresid(mod_5)
plotresid(mod_6)
plotresid(mod_7)
plotresid(mod_8)
plotresid(mod_9)
plotresid(mod_10)
plotresid(mod_11)
plotresid(mod_12)
plotresid(mod_13)
plotresid(mod_14)
plotresid(mod_15)
plotresid(mod_16)
plotresid(mod_17)
plotresid(mod_18)
plotresid(mod_19)
plotresid(mod_20)
plotresid(mod_21)
plotresid(mod_22)
plotresid(mod_22)
plotresid(mod_23)
plotresid(mod_24)
plotresid(mod_25)
plotresid(mod_26)
plotresid(mod_27)
plotresid(mod_28)
plotresid(mod_29)

```
```{r}
abl_data <- ope_biomasse_glm %>%
  filter(espece == "ABL")
mod_1.1 <- glmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                     sta_id),
               family = gaussian(link = "identity"),
               data = abl_data)
mod_1.2 <- glmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                     sta_id),
               family = gaussian(link = "log"),
               data = abl_data)
# mod_1.3 <- glmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
#                                                                                      sta_id),
#                family = gaussian(link = "inverse"),
#                data = abl_data)

mod_1.4 <- glmer(valeur ~ log(annee) + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                     sta_id),
               family = gaussian(link = "identity"),
               data = abl_data)

mod_1.5 <- glmer(valeur ~ sqrt(annee) + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                     sta_id),
               family = gaussian(link = "identity"),
               data = abl_data)

mod_1.5 <- glmer(valeur ~ sqrt(annee) + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                     sta_id),
               family = gaussian(link = "identity"),
               data = abl_data)


```

```{r}
plotresid(mod_1.1)
plotresid(mod_1.2)
# plotresid(mod_1.3)
plotresid(mod_1.4)
plotresid(mod_1.5)
```
```{r}
library(mgcv)
mod_gamm_1.1 <- gamm(
  valeur ~ s(annee) + offset(
    log(ope_surface_calculee))  + pro_libelle,
    random = list(sta_id =  ~ 1),
    data = abl_data,
    family = gaussian(link = "identity"),
    method = "REML"
  )
```
```{r}
ggplot(abl_data, aes(x = annee, y = valeur)) +
  geom_point()
```
```{r, semivariogramme pour tester autocorrelation spatiale avec uniquement la variable reponse}
library(gstat)
var <- variogram(valeur ~ annee + ope_surface_calculee, locations = ~ x+y, data = abl_data)
var
plot(var, col= "black")
```
```{r}
acf(residuals(mod_gamm_1.1$lme, type = "normalized"), lag.max = 20)

```
```{r}
k.check(mod_gamm_1.1$gam)
appraise(mod_gamm_1.1$gam)

```
```{r}
gam.check(mod_gamm_1.1$gam)
```



```{r}
draw(mod_gamm_1.1, residuals = T)

```

## Application du modèle GLMM avec les biomasses

  Afin d'exécuter l'analyse, deux fonctions ont été créer : *zip_glmm_calcul_modele* (en charge de calculer les valeurs des modèles ZIPGLMM) et *zip_glmm_application_modele* (le but étant d'excécuter la fonction *zip_glm_calcul_modele* pour l'ensemble des combinaisons *periodes-especes-stade-indicateur*). Les analyses prennent en considération les données à l'échelle de l'opération. 


```{r Exécution fonction GLMM biomasse}
glmm_resultats <- glmm_application_modele(data = ope_biomasse_glm,
                                          liste_periodes = liste_periodes_etude)
```


```{r Mise en forme sortie fonction GLMM biomasse}
glmm_resultats_annee <- glmm_resultats %>% 
  filter(row_name == "annee") %>% 
  select(periode,
         esp_code_alternatif,
         p_value,
         Estimate,
         sig, 
         family) %>% 
  mutate(trend = case_when(
      Estimate > 0 ~ "\U2197",
      Estimate < 0 ~ "\U2198",
      TRUE ~ "."))
```


```{r Sélection des données résultats GLMM biomasse}
# Filtrer les données selon les critères spécifiés
glmm_resultats_format <- glmm_resultats_annee %>%
  filter((esp_code_alternatif == "ANG" & periode == "2007-2025") |
    (esp_code_alternatif %in% c(c("ABL","BAF","BLN","BRB", "BRE","BRO","CCO","CHA","CHE", "EPI", "EPT", "GAR", "GOU","HOT","LOF",   "LOR", "LOT","LPM" ,"LPP", "OBR", "PER", "ROT", "SAT", "SPI", "TAN", "TOX", "TRF","VAI","VAN","VAR")) & periode == "2007-2025"))

```

Construction d'un tableau récapitulatif des résultats du modèle GLMM :

```{r}
df_glmm_resultats <- glmm_resultats_format %>%
  select(esp_code_alternatif, periode, trend, sig, Estimate, family) %>%
  mutate(Estimate = round(Estimate, digits = 2)) %>%
  rename(
    "Code Espèce" = esp_code_alternatif,
    "Periode" = periode,
    "Tendance" = trend,
    "Coefficient" = Estimate,
    "Significativité" = sig,
    "Famille de distribution" = family
  ) %>%
  arrange(`Code Espèce`) # Ordonner par espèce, indicateur et stade

# Lignes positives et significatives
i_vert  <- which(
  df_glmm_resultats$Tendance == "\U2197" &
    df_glmm_resultats$Significativité %in% c("*", "**", "***")
)
# Lignes négatives et significatives
i_rouge <- which(
  df_glmm_resultats$Tendance == "\U2198" &
    df_glmm_resultats$Significativité %in% c("*", "**", "***")
)

tab_glmm_resultats <- df_glmm_resultats %>%
  flextable::flextable() %>%
  flextable::autofit() %>%
  flextable::colformat_int(j = c(1, 3), big.mark = " ") %>%
  flextable::theme_vanilla() %>%
  flextable::font(fontname = "calibri") %>%
  flextable::fontsize(size = 13) %>%
  flextable::bg(i = i_vert, bg = "darkolivegreen3") %>%   # vert clair
  flextable::bg(i = i_rouge, bg = "coral3") %>%   # rouge clair
  flextable::align(align = "center") %>%
  flextable::set_caption("Synthèse des tendances des biomasses des populations de poissons d'eau douce de BFC")

tab_glmm_resultats
```




```{r Creation du df par especes avec biomasses total par année}
total_valeur_esp <- ope_biomasse_glm %>%
  group_by(annee, espece, indicateur) %>%
  summarise(biomasse_total = sum(valeur), .groups = "drop") %>%
  select(espece, annee, biomasse_total, indicateur)
```

```{r graphique evolution biomasses par annee  lm, fig.height = 30, fig.width = 15}

biomasse_total_esp <- ggplot(total_valeur_esp, aes(x = annee, y = biomasse_total)) +
  facet_wrap2(
    espece ~ indicateur,
    ncol = 3,
    scales = "free_y",
    axes = "all",
    trim_blank = T
  ) +
  geom_point(shape = 21, size = 2) +
  geom_line(linetype = 3, linewidth = 0.5) +
  labs(x = "Années", y = "Biomasse total") +
  geom_smooth(method = "lm", se = F,color = "darkred") +
  theme(
    strip.background = element_rect(color = "grey85", fill = "grey85"),
    legend.position = "bottom",
    panel.background = element_rect(fill = "grey95"),
    panel.grid.major = element_line(color = "#faffff", size = 0.1),
    panel.grid.minor = element_line(color = "#faffff")
  ) +
  geom_vline(
    data = . %>% filter(espece %in% c("ABL","BAF","BLN","BRB", "BRE","BRO","CCO","CHA","CHE", "EPI", "EPT", "GAR", "GOU","HOT","LOF","LOR", "LOT", "OBR", "PER", "ROT", "SAT", "SPI", "TAN", "TOX", "TRF","VAI","VAN","VAR") &
                          annee == 2015),
    aes(xintercept = 2015),
    linetype = "dotted",
    color = "red",
    size = 1
  )

biomasse_total_esp
```
## Pré-traitements GLMM Occurences

```{r Etablir les périodes etudes GLMM occurences}
liste_periodes_etude <- list(c(2007, 2025), 
                             c(2015, 2025)) # Liste des périodes (chaque période est un vecteur avec l'année de début et l'année de fin) 
```


```{r preparation du df pour glmm occurences}
# preparer la table pour jointure
ope_selection <- ope_selection %>%
  dplyr::select(ope_id, pop_id, sta_id)

ope_biomasse_glm <- ope_indicateur %>%
  mef_ajouter_type_protocole() %>%
  mef_ajouter_ope_date() %>%
  mef_ajouter_surf_calc() %>%
  # mef_ajouter_objectif() %>%  # Ajout  des objectifs de pêches
  inner_join(y = ope_selection, by = c("ope_id", "annee", "pop_id")) %>%
  # left_join(coord_sta_id, by = "pop_id") %>%
  mutate(
    valeur = as.numeric(valeur) ,
    annee = as.numeric(annee),
    # annee_an = annee,
    ope_surface_calculee = as.numeric(ope_surface_calculee) ,
    pro_libelle = as.factor(pro_libelle),
    pop_id = as.factor(pop_id),
    sta_id = as.factor(sta_id),
    # obj_libelle = as.factor(obj_libelle),
    # indicateur = "effectif_total"
    ) %>%
  # ) %>% # creation d'une colonne pour la période (ex : 2007-2008)
  # mutate(annee = {
  #   debut <- floor((annee - annee_debut_periode) / 2) * 2 + annee_debut_periode
  #   paste0(debut)
  # }, annee = as.numeric(annee)) %>%
  # mutate(julian = lubridate::yday(ope_date)) %>% #  Ajout de la date de pêche codée de 1 à 365
  select(
    ope_id,
    valeur,
    esp_code_alternatif,
    annee,
    ope_surface_calculee,
    pro_libelle,
    pop_id,
    sta_id,
    indicateur,
    # x,
    # y,
    ope_date,
    # julian,
    # obj_libelle
  ) %>%
  rename(espece = esp_code_alternatif) %>%
  filter(indicateur == "occurence")
```

## Application du modèle GLMM avec les occurences

  Afin d'exécuter l'analyse, deux fonctions ont été créer : *zip_glmm_calcul_modele* (en charge de calculer les valeurs des modèles ZIPGLMM) et *zip_glmm_application_modele* (le but étant d'excécuter la fonction *zip_glm_calcul_modele* pour l'ensemble des combinaisons *periodes-especes-stade-indicateur*). Les analyses prennent en considération les données à l'échelle de l'opération. 


```{r Exécution fonction glmm occurences}
# glmm_resultats <- glmm_application_modele(data = ope_biomasse_glm,
#                                           liste_periodes = liste_periodes_etude)
```


```{r Mise en forme sortie fonction GLMM occurences}
# glmm_resultats_annee <- glmm_resultats %>% 
#   filter(row_name == "annee") %>% 
#   select(periode,
#          esp_code_alternatif,
#          p_value,
#          Estimate,
#          sig, 
#          family) %>% 
#   mutate(trend = case_when(
#       Estimate > 0 ~ "\U2197",
#       Estimate < 0 ~ "\U2198",
#       TRUE ~ "."))
```


```{r Sélection des données résultats glmm occurences}
# Filtrer les données selon les critères spécifiés
# glmm_resultats_format <- glmm_resultats_annee %>%
#   filter((esp_code_alternatif == "ANG" & periode == "2007-2025") |
#     (esp_code_alternatif %in% c(c("ABL","BAF","BLN","BRB", "BRE","BRO","CCO","CHA","CHE", "EPI", "EPT", "GAR", "GOU","HOT","LOF",   "LOR", "LOT","LPM" ,"LPP", "OBR", "PER", "ROT", "SAT", "SPI", "TAN", "TOX", "TRF","VAI","VAN","VAR")) & periode == "2007-2025"))

```

Construction d'un tableau récapitulatif des résultats du modèle GLMM :

Pour construire le jeu de donnée occurences, on utilise comme base le df **ope_indicateur** avec comme indicateur *l'effectif total*. 
Les combinaisons de 
```{r construction du jeu de donnée occurences}

#

```


```{r}
# df_glmm_resultats <- glmm_resultats_format %>%
#   select(esp_code_alternatif, periode, trend, sig, Estimate, family) %>%
#   mutate(Estimate = round(Estimate, digits = 2)) %>%
#   rename(
#     "Code Espèce" = esp_code_alternatif,
#     "Periode" = periode,
#     "Tendance" = trend,
#     "Coefficient" = Estimate,
#     "Significativité" = sig,
#     "Famille de distribution" = family
#   ) %>%
#   arrange(`Code Espèce`) # Ordonner par espèce, indicateur et stade
# 
# # Lignes positives et significatives
# i_vert  <- which(
#   df_glmm_resultats$Tendance == "\U2197" &
#     df_glmm_resultats$Significativité %in% c("*", "**", "***")
# )
# # Lignes négatives et significatives
# i_rouge <- which(
#   df_glmm_resultats$Tendance == "\U2198" &
#     df_glmm_resultats$Significativité %in% c("*", "**", "***")
# )
# 
# tab_glmm_resultats <- df_glmm_resultats %>%
#   flextable::flextable() %>%
#   flextable::autofit() %>%
#   flextable::colformat_int(j = c(1, 3), big.mark = " ") %>%
#   flextable::theme_vanilla() %>%
#   flextable::font(fontname = "calibri") %>%
#   flextable::fontsize(size = 13) %>%
#   flextable::bg(i = i_vert, bg = "darkolivegreen3") %>%   # vert clair
#   flextable::bg(i = i_rouge, bg = "coral3") %>%   # rouge clair
#   flextable::align(align = "center") %>%
#   flextable::set_caption("Synthèse des tendances des biomasses des populations de poissons d'eau douce de BFC")
# 
# tab_glmm_resultats
```

## Pré-traitements ZIP_GLMM Effectifs

  Afin d'appliquer les modèles GLMM, il est nécessaire de construire un jeu de données contenant les valeurs des effectifs, les espèces, les stades, les *pop_id*, mais aussi les types de réseaux, les surfaces d'opérations et les protocoles de pêches. Nous utilisons dans un premier temps, le jeu de données relatifs aux effectifs des opérations de pêches (*ope_effectifs*) car nous souhaitons observer les effets des réseaux et des protocoles sur les résultats des opérations. 


```{r Etablir les périodes etudes ZIP_GLMM}
liste_periodes_etude <- list(c(2007, 2025), 
                             c(2015, 2025)) # Liste des périodes (chaque période est un vecteur avec l'année de début et l'année de fin) 
```

Nous construisons un jeu de données complet :

```{r preparation du df pour zip glmm effectif, fig.height = 30, fig.width = 15}
# Pour creer la colonne periode : on renseigne le début de période souhaiter : ici 2007
annee_debut_periode <- 2007

# preparer la table pour jointure
ope_selection <- ope_selection %>%
  dplyr::select(ope_id, pop_id, sta_id)

# Ajout des coordonnées pour jointure avec df pour future analyse autocorrelation spatiale
coord_sta_id <- point_prelevement %>%
  filter(pop_id %in% pop_serie_tempo) %>%
  rename(x = pop_coordonnees_x, y = pop_coordonnees_y) %>%
  select(pop_id, x, y)

ope_effectif_glm <- esp_ope_selection %>%
  mef_ajouter_type_protocole() %>%
  mef_ajouter_ope_date() %>%
  # mef_ajouter_objectif() %>%  # Ajout  des objectifs de pêches
  left_join(ope_selection, by = "ope_id") %>%
  rename(valeur = lop_effectif, annee = annee.x) %>%
  left_join(coord_sta_id, by = "pop_id") %>%
  dplyr::group_by(sta_id, ope_id, esp_code_alternatif, annee) %>%
  summarise(valeur = sum(valeur)) %>%
  select(
    sta_id,
    ope_id,
    esp_code_alternatif,
    annee,
    valeur,
    ope_surface_calculee,
    x,
    y,
    ope_date
  ) %>%
  mutate(
    valeur = as.numeric(valeur) ,
    annee = as.numeric(annee),
    # annee_an = annee,
    ope_surface_calculee = as.numeric(ope_surface_calculee) ,
    pro_libelle = as.factor(pro_libelle),
    pop_id = as.factor(pop_id),
    sta_id = as.factor(sta_id),
    # obj_libelle = as.factor(obj_libelle),
    indicateur = "effectif_total"
  ) %>%
  # ) %>% # creation d'une colonne pour la période (ex : 2007-2008)
  # mutate(annee = {
  #   debut <- floor((annee - annee_debut_periode) / 2) * 2 + annee_debut_periode
  #   paste0(debut)
  # }, annee = as.numeric(annee)) %>%
  mutate(julian = lubridate::yday(ope_date)) %>% #  Ajout de la date de pêche codée de 1 à 365
  select(
    ope_id,
    valeur,
    esp_code_alternatif,
    annee,
    ope_surface_calculee,
    pro_libelle,
    pop_id,
    sta_id,
    indicateur,
    x,
    y,
    ope_date,
    julian
    # obj_libelle
  ) %>%
  rename(espece = esp_code_alternatif) %>%  
  ungroup() 

```

# CORRECTION ERREUR DF 

```{r Test pour erreur de surdispersion}
# preparer la table pour jointure
ope_selection <- ope_selection %>%
  dplyr::select(ope_id, pop_id, sta_id)

# prepéarer la table pour jointure 
esp_ope_selection_join <- esp_ope_selection %>% 
  select(ope_id, ope_surface_calculee) %>% 
  distinct(ope_id, .keep_all = TRUE)


ope_effectif_glm <- ope_effectif %>%
  mef_ajouter_type_protocole() %>%
  left_join(ope_selection, by = "ope_id") %>%
  inner_join(esp_ope_selection_join, by = "ope_id") %>%
  rename(espece = esp_code_alternatif)
  

```

```{r Test pour erreur de surdispersion avec coord}
# preparer la table pour jointure
ope_selection <- ope_selection %>%
  dplyr::select(ope_id, pop_id, sta_id)

# Ajout des coordonnées pour jointure avec df pour future analyse autocorrelation spatiale
coord_sta_id <- point_prelevement %>%
  filter(pop_id %in% pop_serie_tempo) %>%
  rename(x = pop_coordonnees_x, y = pop_coordonnees_y) %>%
  select(pop_id, x, y) %>%
  mutate(pop_id = as.factor(pop_id))

# prepéarer la table pour jointure 
esp_ope_selection_join <- esp_ope_selection %>% 
  select(ope_id, ope_surface_calculee) %>% 
  distinct(ope_id, .keep_all = TRUE)


ope_effectif_glm <- ope_effectif %>%
  mef_ajouter_type_protocole() %>%
  left_join(ope_selection, by = "ope_id") %>%
  inner_join(esp_ope_selection_join, by = "ope_id") %>%
  mutate(
    valeur = as.numeric(valeur) ,
    annee = as.numeric(annee),
    ope_surface_calculee = as.numeric(ope_surface_calculee),
    pro_libelle = as.factor(pro_libelle),
    pop_id = as.factor(pop_id),
    sta_id = as.factor(sta_id)
  ) %>%
  left_join(coord_sta_id, by = "pop_id") %>% 
  mutate(x = as.numeric(x), 
         y = as.numeric(y))%>%
  rename(espece = esp_code_alternatif)
  
```

```{r Test pour erreur de surdispersion avec coord et absences}
# preparer la table pour jointure
ope_selection <- ope_selection %>%
  dplyr::select(ope_id, pop_id, sta_id)

# Ajout des coordonnées pour jointure avec df pour future analyse autocorrelation spatiale
coord_sta_id <- point_prelevement %>%
  filter(pop_id %in% pop_serie_tempo) %>%
  rename(x = pop_coordonnees_x, y = pop_coordonnees_y) %>%
  select(pop_id, x, y) %>%
  mutate(pop_id = as.factor(pop_id))

# prepéarer la table pour jointure 
esp_ope_selection_join <- esp_ope_selection %>% 
  select(ope_id, ope_surface_calculee) %>% 
  distinct(ope_id, .keep_all = TRUE)


ope_effectif_glm <- ope_effectif_absences %>%
  mef_ajouter_type_protocole() %>%
  left_join(ope_selection, by = "ope_id") %>%
  inner_join(esp_ope_selection_join, by = "ope_id") %>%
  mutate(
    valeur = as.numeric(valeur) ,
    annee = as.numeric(annee),
    ope_surface_calculee = as.numeric(ope_surface_calculee),
    pro_libelle = as.factor(pro_libelle),
    pop_id = as.factor(pop_id),
    sta_id = as.factor(sta_id)
  ) %>%
  left_join(coord_sta_id, by = "pop_id") %>% 
  mutate(x = as.numeric(x), 
         y = as.numeric(y))%>%
  rename(espece = esp_code_alternatif)
  
```

## Test modele sans ziformula ZIPGLMM

```{r}
truite_data <- ope_effectif_glm %>% 
  filter(espece == "TRF")
```


```{r}
hist(truite_data$valeur)
```
```{r}
vairon_data <- ope_effectif_glm %>% 
  filter(espece == "VAI")
```

```{r}
epi_data <- ope_effectif_glm %>% 
  filter(espece == "EPI")
```

```{r}
ggplot(data = epi_data) +
  geom_histogram(mapping = aes(x = valeur)) +
  theme_minimal()
```
```{r histogramme distribution valeur effectif par espece, fig.height = 60, fig.width = 30}
histogram_total_esp <- ggplot(data = ope_effectif_glm, aes(x = valeur)) +
  facet_wrap2(
    espece ~ indicateur,
    ncol = 3,
    scales = "free",
    axes = "all",
    trim_blank = T
  ) +
  geom_histogram(mapping = aes(x = valeur), position = "dodge", bins = 30) +
  theme(
    strip.background = element_rect(color = "grey85", fill = "grey85"),
    legend.position = "bottom",
    panel.background = element_rect(fill = "grey95")
    # panel.grid.major = element_line(color = "#faffff", size = 0.1),
    # panel.grid.minor = element_line(color = "#faffff")
  )

histogram_total_esp
  
```


```{r histogramme distribution valeur effectif par espece avec seuil, fig.height = 60, fig.width = 15}
library(ggplot2)
library(ggh4x)
library(dplyr)

# Calcul des stats par espèce pour annoter
# stats_esp <- ope_effectif_glm %>%
#   group_by(espece) %>%
#   summarise(
#     pct_zero  = mean(valeur == 0) * 100,
#     pct_un    = mean(valeur == 1) * 100,
#     pct_zero1 = mean(valeur <= 1) * 100,
#     n         = n(),
#     .groups   = "drop"
#   ) %>%
#   mutate(
#     label = paste0(
#       "n=", n,
#       "  0s:", round(pct_zero), "%",
#       "  1s:", round(pct_un), "%"
#     ),
#     # Diagnostic automatique
#     modele = case_when(
#       pct_zero  > 70 ~ "Zero-inflated",
#       pct_zero1 > 60 ~ "Hurdle",
#       pct_zero  > 40 ~ "ZIP / ZINB",
#       TRUE           ~ "Poisson / NB"
#     )
#   )

histogram_esp <- ggplot(data = ope_effectif_glm, aes(x = valeur)) +
  facet_wrap2(
    ~ espece,
    ncol   = 3,
    scales = "free",
    axes   = "all",
    trim_blank = TRUE
  ) +
  geom_histogram(bins = 30, fill = "steelblue4", color = "white", linewidth = 0.2) +
  geom_vline(xintercept = 0, color = "firebrick", linetype = "dashed", linewidth = 0.5) +
  geom_vline(xintercept = 1, color = "darkorange", linetype = "dotted",  linewidth = 0.5) +
  # Annotation stats + diagnostic dans chaque facette
  # geom_text(
  #   data    = stats_esp,
  #   mapping = aes(x = Inf, y = Inf, label = label),
  #   hjust   = 1.05, vjust = 1.5,
  #   size    = 2.8, color = "grey30"
  # ) +
  # geom_label(
  #   data    = stats_esp,
  #   mapping = aes(x = Inf, y = Inf, label = modele),
  #   hjust   = 1.05, vjust = 3.5,
  #   size    = 2.5, fontface = "bold",
  #   fill    = "white", color = "steelblue4", label.size = 0.3
  # ) +
  scale_x_continuous(expand = expansion(mult = c(0, 0.05))) +
  scale_y_continuous(expand = expansion(mult = c(0, 0.1))) +
  labs(
    x     = "Effectif",
    y     = "Fréquence",
    title = "Distribution des effectifs par espèce",
    # caption = "Rouge = 0  |  Orange = 1  |  Label : % zéros, % uns → orientation modèle GLMM"
  ) +
  theme_minimal(base_size = 11) +
  theme(
    strip.background = element_rect(fill = "grey88", color = NA),
    strip.text       = element_text(face = "bold", size = 9),
    panel.background = element_rect(fill = "grey97", color = NA),
    panel.grid.major = element_line(color = "grey90"),
    panel.grid.minor = element_blank(),
    plot.caption     = element_text(size = 8, color = "grey50")
  )

histogram_esp
```
```{r}
truite_data <- ope_effectif_glm %>% 
  filter(espece == "ABL")
```



```{r, construction des modeles glmmzi}
# library(glmmTMB)
mod_zi_1.0 <- glmmTMB(
  valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                        sta_id) ,
  data = truite_data,
  family = poisson(link = "log")
)
mod_zi_1.1 <- glmmTMB(
  valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                        sta_id) ,
  family = nbinom2(link = "log"),
  data = truite_data
)
mod_zi_1.2 <- glmmTMB(
  valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                        sta_id) ,
  family = truncated_poisson(link = "log"),
  data = truite_data, 
  ziformula = ~1
)
mod_zi_1.3 <- glmmTMB(
  valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                        sta_id) ,
  family = truncated_nbinom2(link = "log"),
  data = truite_data, 
  ziformula = ~1
)

```

On selectionne à l'aide du critère AIC le meilleur modèle c'est à dire le modèle avec le plus petit AIC. 

```{r, selection du meilleur modeles}
AIC(mod_zi_1.0,
    mod_zi_1.1,
    mod_zi_1.2,
    mod_zi_1.3
    )
```

```{r}
plotresid(mod_zi_1.0)
plotresid(mod_zi_1.1)
plotresid(mod_zi_1.2)
plotresid(mod_zi_1.3)
```

```{r}
summary(mod_zi_1.1)
summary(mod_zi_1.0)

```




```{r}
res_mod_zi_1.0 <- DHARMa::simulateResiduals(mod_zi_1.0)
plot(res_mod_zi_1.0)
DHARMa::testDispersion(res_mod_zi_1.0)

res_mod_zi_1.1 <- DHARMa::simulateResiduals(mod_zi_1.1)
plot(res_mod_zi_1.1)
DHARMa::testDispersion(res_mod_zi_1.1)

res_mod_zi_1.2 <- DHARMa::simulateResiduals(mod_zi_1.2)
plot(res_mod_zi_1.2)
DHARMa::testDispersion(res_mod_zi_1.2)

DHARMa::testOutliers(res_mod_zi_1.0 ,type = "bootstrap")
res_mod_zi_1.3 <- DHARMa::simulateResiduals(mod_zi_1.3, n = 1000)
plot(res_mod_zi_1.3)
DHARMa::testDispersion(res_mod_zi_1.3)
DHARMa::testZeroInflation(res_mod_zi_1.3)


```

```{r}
mod_glmm_qbn <- glmer.nb(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle+ (1|sta_id),  data = truite_data,  family = poisson(link = "log")
)

```


```{r}
plotresid(mod_glmm_qbn)
res_mod_glmm_qbn <- DHARMa::simulateResiduals(mod_glmm_qbn)
plot(res_mod_glmm_qbn)
```

```{r}
summary(mod_glmm_qbn)
```

```{r plot des effectif par annee et par espece, fig.height = 60, fig.width = 30}
ggplot(data = ope_effectif_glm, mapping = aes(x = annee, y = valeur)) +
  facet_wrap2(
    espece ~ indicateur,
    ncol = 3,
    scales = "free_y",
    axes = "all",
    trim_blank = TRUE
  ) +
  geom_point(shape = 21, size = 2, color = "royalblue")+
  # geom_line(linetype = 3, linewidth = 0.5) +
  geom_smooth(method = "glm",method.args = list(family = "poisson"),
              se = T,
              color = "darkblue") +  
  labs(x = "Années", y = "Effectif total") +
  theme(
    strip.background = element_rect(color = "grey85", fill = "grey85"),
    legend.position = "bottom",
    panel.background = element_rect(fill = "grey95"),
    panel.grid.major = element_line(color = "#faffff", size = 0.1),
    panel.grid.minor = element_line(color = "#faffff")
  ) 
```

```{r boxplot des effectif par protocoles et par espece, fig.height = 60, fig.width = 30}
ggplot(data = ope_effectif_glm, mapping = aes(x = pro_libelle, y = valeur)) +
  facet_wrap2(
    espece ~ indicateur,
    ncol = 3,
    scales = "free_y",
    axes = "all",
    trim_blank = T
  ) +
  geom_boxplot(color = "royalblue") +
  labs(x = "Protocoles de pêche", y = "Effectif") +
  theme(
    strip.background = element_rect(color = "grey85", fill = "grey85"),
    legend.position = "bottom",
    panel.background = element_rect(fill = "grey95")
    # panel.grid.major = element_line(color = "#faffff", size = 0.1),
    # panel.grid.minor = element_line(color = "#faffff")
  )
```


```{r}
summary(mod_zi_1.1)
```
# Interpretation test : model avec truite : 


```{r}
mean(truite_data$valeur == 0)
```

```{r}
library(DHARMa)

sim <- simulateResiduals(mod_zi_1.1, n = 1000)
plot(sim)

# Test explicite de dispersion
testDispersion(sim)
# p > 0.05 → ni surdispersion ni sous-dispersion résiduelle ✅

# Test zero-inflation résiduelle
testZeroInflation(sim)
# p > 0.05 → les zéros sont bien modélisés ✅
```


```{r}
# Interprétation de l'estimate annee dans votre modèle actuel
# En attendant la correction du modèle, votre résultat donne déjà :
# annee : Estimate = -0.01664   p = 0.000139 ***
# Sur l'échelle log, cela correspond à une diminution annuelle de :
(exp(-0.016635) - 1) * 100
# -1.65% par an
# Sur 20 ans : exp(-0.016635 * 20) - 1 = -28.4%
# L'abondance de la truite diminue significativement de ~1.6% par an (p < 0.001), soit une réduction estimée d'environ 28% sur la période 2006–2025."
```







```{r plotresid des modèles glmm zero inflated, fig.height = 10, fig.width = 20}
library(ggplotify)
library(ggpubr)
res_mod_zi_1.0 <- as.ggplot(~ plotresid(mod_zi_1.0))
res_mod_zi_1.1 <- as.ggplot(~ plotresid(mod_zi_1.1))
res_mod_zi_1.2 <- as.ggplot(~ plotresid(mod_zi_1.2))
res_mod_zi_1.3 <- as.ggplot(~ plotresid(mod_zi_1.3))

ggarrange(
  res_mod_zi_1.0,
  res_mod_zi_1.1,
  res_mod_zi_1.2,
  res_mod_zi_1.3,
  ncol = 2,
  nrow = 2,
  labels = c("poisson", "nbinom2", "truncated_poisson", "truncated_nbinom2"),
  font.label = list(
    size = 13,
    color = "darkred",
    face = "bold",
    family = NULL
  ),
  hjust = -0.1,
  vjust = 1.6,
  align = c("none", "h", "v", "hv")
)
```

```{r}
summary(mod_zi_1.0)
summary(mod_zi_1.1)
summary(mod_zi_1.2)
summary(mod_zi_1.3)
```
```{r}
mod_zi_1.3 <- glmmTMB(
  valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 + annee |
                                                                        sta_id) ,
  family = truncated_nbinom2(link = "log"),
  data = truite_data
)
```

```{r}
summary(mod_zi_1.3)
```

```{r}
mod_glmm <- glmer(
  valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 + annee |
                                                                        sta_id) ,
  family = poisson(link = "log"),
  data = truite_data
)
```

```{r}
plotresid(mod_glmm)
overdisp.glmer(mod_glmm)
summary(mod_glmm)
```

## Application du modèle GLMM avec les effectifs

  Afin d'exécuter l'analyse, deux fonctions ont été créer : *zip_glmm_calcul_modele* (en charge de calculer les valeurs des modèles ZIPGLMM) et *zip_glmm_application_modele* (le but étant d'excécuter la fonction *zip_glm_calcul_modele* pour l'ensemble des combinaisons *periodes-especes-stade-indicateur*). Les analyses prennent en considération les données à l'échelle de l'opération. 
```{r}
source(file = "../R/zip_glmm_calcul_modele.R")
source(file = "../R/zip_glmm_application_modele.R")
```


```{r Exécution fonction ZIP_GLMM}
zip_glmm_resultats <- zip_glmm_application_modele(data = ope_effectif_glm,
                                          liste_periodes = liste_periodes_etude)
```


```{r Mise en forme sortie fonction ZIP_GLMM}
zip_glmm_resultats_annee <- zip_glmm_resultats %>% 
  filter(row_name == "annee") %>% 
  select(periode,
         esp_code_alternatif,
         p_value,
         Estimate,
         sig, 
         family) %>% 
  mutate(trend = case_when(
      Estimate > 0 ~ "\U2197",
      Estimate < 0 ~ "\U2198",
      TRUE ~ "."))
```


```{r Sélection des données résultats ZIP_GLMM}
# Filtrer les données selon les critères spécifiés
zip_glmm_resultats_format <- zip_glmm_resultats_annee %>%
  filter((esp_code_alternatif == "ANG" & periode == "2007-2025") |
    (esp_code_alternatif %in% c(c("ABL","BAF","BLN","BRB", "BRE","BRO","CCO","CHA","CHE", "EPI", "EPT", "GAR", "GOU","HOT","LOF",   "LOR", "LOT","LPM" ,"LPP", "OBR", "PER", "ROT", "SAT", "SPI", "TAN", "TOX", "TRF","VAI","VAN","VAR")) & periode == "2007-2025"))

```

Construction d'un tableau récapitulatif des résultats du modèle GLMM :

```{r}
df_zip_glmm_resultats <- zip_glmm_resultats_format %>%
  select(esp_code_alternatif, periode, trend, sig, Estimate, family) %>%
  mutate(Estimate = round(Estimate, digits = 2)) %>%
  rename(
    "Code Espèce" = esp_code_alternatif,
    "Periode" = periode,
    "Tendance" = trend,
    "Coefficient" = Estimate,
    "Significativité" = sig,
    "Famille de distribution" = family
  ) %>%
  arrange(`Code Espèce`) # Ordonner par espèce, indicateur et stade

# Lignes positives et significatives
i_vert  <- which(
  df_zip_glmm_resultats$Tendance == "\U2197" &
    df_zip_glmm_resultats$Significativité %in% c("*", "**", "***")
)
# Lignes négatives et significatives
i_rouge <- which(
  df_zip_glmm_resultats$Tendance == "\U2198" &
    df_zip_glmm_resultats$Significativité %in% c("*", "**", "***")
)

tab_zip_glmm_resultats <- df_zip_glmm_resultats %>%
  flextable::flextable() %>%
  flextable::autofit() %>%
  flextable::colformat_int(j = c(1, 3), big.mark = " ") %>%
  flextable::theme_vanilla() %>%
  flextable::font(fontname = "calibri") %>%
  flextable::fontsize(size = 13) %>%
  flextable::bg(i = i_vert, bg = "darkolivegreen3") %>%   # vert clair
  flextable::bg(i = i_rouge, bg = "coral3") %>%   # rouge clair
  flextable::align(align = "center") %>%
  flextable::set_caption("Synthèse des tendances des effectifs des populations de poissons d'eau douce de BFC")

tab_zip_glmm_resultats
```

## Application du modèle GLMM avec les effectifs ET LES ABSENCES

  Afin d'exécuter l'analyse, deux fonctions ont été créer : *zip_glmm_calcul_modele* (en charge de calculer les valeurs des modèles ZIPGLMM) et *zip_glmm_application_modele* (le but étant d'excécuter la fonction *zip_glm_calcul_modele* pour l'ensemble des combinaisons *periodes-especes-stade-indicateur*). Les analyses prennent en considération les données à l'échelle de l'opération. 

```{r Test pour erreur de surdispersion avec coord}
# preparer la table pour jointure
ope_selection <- ope_selection %>%
  dplyr::select(ope_id, pop_id, sta_id)

# Ajout des coordonnées pour jointure avec df pour future analyse autocorrelation spatiale
coord_sta_id <- point_prelevement %>%
  filter(pop_id %in% pop_serie_tempo) %>%
  rename(x = pop_coordonnees_x, y = pop_coordonnees_y) %>%
  select(pop_id, x, y) %>%
  mutate(pop_id = as.factor(pop_id))

# prepéarer la table pour jointure 
esp_ope_selection_join <- esp_ope_selection %>% 
  select(ope_id, ope_surface_calculee) %>% 
  distinct(ope_id, .keep_all = TRUE)


ope_effectif_glm <- ope_effectif_absences %>%
  mef_ajouter_type_protocole() %>%
  left_join(ope_selection, by = "ope_id") %>%
  inner_join(esp_ope_selection_join, by = "ope_id") %>%
  mutate(
    valeur = as.numeric(valeur) ,
    annee = as.numeric(annee),
    ope_surface_calculee = as.numeric(ope_surface_calculee),
    pro_libelle = as.factor(pro_libelle),
    pop_id = as.factor(pop_id),
    sta_id = as.factor(sta_id)
  ) %>%
  left_join(coord_sta_id, by = "pop_id") %>% 
  mutate(x = as.numeric(x), 
         y = as.numeric(y))%>%
  rename(espece = esp_code_alternatif)
  
```

```{r Exécution fonction ZIP_GLMM}
zip_glmm_resultats <- zip_glmm_application_modele(data = ope_effectif_glm,
                                          liste_periodes = liste_periodes_etude)
```


```{r Mise en forme sortie fonction ZIP_GLMM}
zip_glmm_resultats_annee <- zip_glmm_resultats %>% 
  filter(row_name == "annee") %>% 
  select(periode,
         esp_code_alternatif,
         p_value,
         Estimate,
         sig, 
         family) %>% 
  mutate(trend = case_when(
      Estimate > 0 ~ "\U2197",
      Estimate < 0 ~ "\U2198",
      TRUE ~ "."))
```


```{r Sélection des données résultats ZIP_GLMM}
# Filtrer les données selon les critères spécifiés
zip_glmm_resultats_format <- zip_glmm_resultats_annee %>%
  filter((esp_code_alternatif == "ANG" & periode == "2007-2025") |
    (esp_code_alternatif %in% c(c("ABL","BAF","BLN","BRB", "BRE","BRO","CCO","CHA","CHE", "EPI", "EPT", "GAR", "GOU","HOT","LOF", "LOR", "LOT","LPM" ,"LPP", "OBR", "PER", "ROT", "SAT", "SPI", "TAN", "TOX", "TRF","VAI","VAN","VAR")) & periode == "2007-2025"))

```

Construction d'un tableau récapitulatif des résultats du modèle GLMM :

```{r}
df_zip_glmm_resultats <- zip_glmm_resultats_format %>%
  select(esp_code_alternatif, periode, trend, sig, Estimate, family) %>%
  mutate(Estimate = round(Estimate, digits = 2)) %>%
  rename(
    "Code Espèce" = esp_code_alternatif,
    "Periode" = periode,
    "Tendance" = trend,
    "Coefficient" = Estimate,
    "Significativité" = sig,
    "Famille de distribution" = family
  ) %>%
  arrange(`Code Espèce`) # Ordonner par espèce, indicateur et stade

# Lignes positives et significatives
i_vert  <- which(
  df_zip_glmm_resultats$Tendance == "\U2197" &
    df_zip_glmm_resultats$Significativité %in% c("*", "**", "***")
)
# Lignes négatives et significatives
i_rouge <- which(
  df_zip_glmm_resultats$Tendance == "\U2198" &
    df_zip_glmm_resultats$Significativité %in% c("*", "**", "***")
)

tab_zip_glmm_resultats <- df_zip_glmm_resultats %>%
  flextable::flextable() %>%
  flextable::autofit() %>%
  flextable::colformat_int(j = c(1, 3), big.mark = " ") %>%
  flextable::theme_vanilla() %>%
  flextable::font(fontname = "calibri") %>%
  flextable::fontsize(size = 13) %>%
  flextable::bg(i = i_vert, bg = "darkolivegreen3") %>%   # vert clair
  flextable::bg(i = i_rouge, bg = "coral3") %>%   # rouge clair
  flextable::align(align = "center") %>%
  flextable::set_caption("Synthèse des tendances des effectifs des populations de poissons d'eau douce de BFC")

tab_zip_glmm_resultats
```
## Verification des conditions d'application du modele 

```{r}
source(file = "../R/adequate_data_for_model.R")

ope_effectif_glm_test <- ope_effectif_glm %>% 
  rename(esp_code_alternatif = espece)
adequate_data_for_model(ope_effectif_glm_test, esp = "BAF")

adequate_data_for_model(ope_effectif_glm_test, esp = c("ABL","ANG","BAF","BLN","BRB", "BRE","BRO","CCO","CHA","CHE", "EPI", "EPT", "GAR", "GOU","HOT","LOF","LOR", "LOT","LPM" ,"LPP", "OBR", "PER", "ROT", "SAT", "SPI", "TAN", "TOX", "TRF","VAI","VAN","VAR") )

```

```{r}
table(ope_effectif_glm$espece)

```


```{r}

abl_data <- ope_effectif_glm %>%
  filter(espece == "ABL")
mod_1 <- glmmTMB(
      valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                            sta_id),
      family = truncated_nbinom2(link = "log"),
      data  = abl_data,
      ziformula = ~ 1
    )
ang_data <- ope_effectif_glm %>%
  filter(espece == "ANG")

mod_2 <- glmer.nb(
    valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle +(1 |
                                                                          sta_id),
    data = ang_data,
    family = poisson(link = "log")
  )

baf_data <- ope_effectif_glm %>%
  filter(espece == "BAF")
mod_3 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                              sta_id), data = baf_data)
bln_data <- ope_effectif_glm %>%
  filter(espece == "BLN")
mod_4 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                              sta_id), data = bln_data)

brb_data <- ope_effectif_glm %>%
  filter(espece == "BRB")
mod_5 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                              sta_id), data = brb_data)

bre_data <- ope_effectif_glm %>%
  filter(espece == "BRE")
mod_6 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                              sta_id), data = bre_data)

bro_data <- ope_effectif_glm %>%
  filter(espece == "BRO")
mod_7 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                              sta_id), data = bro_data)

cco_data <- ope_effectif_glm %>%
  filter(espece == "CCO")
mod_8 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                              sta_id), data = cco_data)

cha_data <- ope_effectif_glm %>%
  filter(espece == "CHA")
mod_9 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                              sta_id), data = cha_data)

che_data <- ope_effectif_glm %>%
  filter(espece == "CHE")
mod_10 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = che_data)

epi_data <- ope_effectif_glm %>%
  filter(espece == "EPI")
mod_11 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = epi_data)


ept_data <- ope_effectif_glm %>%
  filter(espece == "EPT")
mod_12 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = ept_data)


gar_data <- ope_effectif_glm %>%
  filter(espece == "GAR")
mod_13 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = gar_data)


gou_data <- ope_effectif_glm %>%
  filter(espece == "GOU")
mod_14 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = gou_data)


hot_data <- ope_effectif_glm %>%
  filter(espece == "HOT")
mod_15 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = hot_data)


lof_data <- ope_effectif_glm %>%
  filter(espece == "LOF")
mod_16 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = lof_data)


lor_data <- ope_effectif_glm %>%
  filter(espece == "LOR")
mod_17 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = lor_data)


lot_data <- ope_effectif_glm %>%
  filter(espece == "LOT")
mod_18 <- glmer.nb(
    valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle +(1 |
                                                                          sta_id),
    data = lot_data,
    family = poisson(link = "log")
  )


lpp_data <- ope_effectif_glm %>%
  filter(espece == "LPP")
mod_19 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = lpp_data)


obr_data <- ope_effectif_glm %>%
  filter(espece == "OBR")
mod_20 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = obr_data)


per_data <- ope_effectif_glm %>%
  filter(espece == "PER")
mod_21 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = per_data)


rot_data <- ope_effectif_glm %>%
  filter(espece == "ROT")
mod_22 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = rot_data)


sat_data <- ope_effectif_glm %>%
  filter(espece == "SAT")
mod_23 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = sat_data)



spi_data <- ope_effectif_glm %>%
  filter(espece == "SPI")
mod_24 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = spi_data)


tan_data <- ope_effectif_glm %>%
  filter(espece == "TAN")
mod_25 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = tan_data)


tox_data <- ope_effectif_glm %>%
  filter(espece == "TOX")
mod_26 <- glmer.nb(
    valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle +(1 |
                                                                          sta_id),
    data = tox_data,
    family = poisson(link = "log")
  )

trf_data <- ope_effectif_glm %>%
  filter(espece == "TRF")
mod_27 <- glmer.nb(
    valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle +(1 |
                                                                          sta_id),
    data = trf_data,
    family = poisson(link = "log")
  )


vai_data <- ope_effectif_glm %>%
  filter(espece == "VAI")
mod_28 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = vai_data)


van_data <- ope_effectif_glm %>%
  filter(espece == "VAN")
mod_29 <- lmerTest::lmer(valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 |
                                                                                               sta_id), data = van_data)

```

```{r}
plotresid(mod_1)
plotresid(mod_2)
plotresid(mod_3)
plotresid(mod_4)
plotresid(mod_5)
plotresid(mod_6)
plotresid(mod_7)
plotresid(mod_8)
plotresid(mod_9)
plotresid(mod_10)
plotresid(mod_11)
plotresid(mod_12)
plotresid(mod_13)
plotresid(mod_14)
plotresid(mod_15)
plotresid(mod_16)
plotresid(mod_17)
plotresid(mod_18)
plotresid(mod_19)
plotresid(mod_20)
plotresid(mod_21)
plotresid(mod_22)
plotresid(mod_22)
plotresid(mod_23)
plotresid(mod_24)
plotresid(mod_25)
plotresid(mod_26)
plotresid(mod_27)
plotresid(mod_28)
plotresid(mod_29)

```



# Sauvegarde

```{r Sauvegarde}
# save(zip_glmm_resultats_format,
#      reg_indicateur_lrr_tendances_lm,
#      reg_indicateur_lrr_tendances_mk_st,
#      tab_glmm_resultats,
#      tab_reg_indicateur_lrr_tendances_lm,
#      tab_reg_indicateur_lrr_tendances_mk_st,
#      file = "../processed_data/mk_glmm_lm_tendances.rda")
```

## TEst avec ajustement modele

```{r}
library(DHARMa)
# install.packages("DHARMa")
library(ggplotify)
library(ggpubr)
```

```{r}
source(file = "../R/calcul_model_test.R")
source(file = "../R/application_modele_test.R")
```

```{r Etablir les périodes etudes ZIP_GLMM}
liste_periodes_etude <- list(c(2007, 2025), 
                             c(2015, 2025)) # Liste des périodes (chaque période est un vecteur avec l'année de début et l'année de fin) 
```

Nous construisons un jeu de données complet :

```{r preparation du df pour zip glmm effectif, fig.height = 30, fig.width = 15}
# Pour creer la colonne periode : on renseigne le début de période souhaiter : ici 2007
# annee_debut_periode <- 2007

# preparer la table pour jointure
ope_selection <- ope_selection %>%
  dplyr::select(ope_id, pop_id, sta_id)

# Ajout des coordonnées pour jointure avec df pour future analyse autocorrelation spatiale
coord_sta_id <- point_prelevement %>%
  filter(pop_id %in% pop_serie_tempo) %>%
  rename(x = pop_coordonnees_x, y = pop_coordonnees_y) %>%
  select(pop_id, x, y)

ope_effectif_glm <- esp_ope_selection %>%
  mef_ajouter_type_protocole() %>%
  mef_ajouter_ope_date() %>%
  # mef_ajouter_objectif() %>%  # Ajout  des objectifs de pêches
  left_join(ope_selection, by = "ope_id") %>%
  rename(valeur = lop_effectif, annee = annee.x) %>%
  left_join(coord_sta_id, by = "pop_id") %>%
  mutate(
    valeur = as.numeric(valeur) ,
    annee = as.numeric(annee),
    # annee_an = annee,
    ope_surface_calculee = as.numeric(ope_surface_calculee) ,
    pro_libelle = as.factor(pro_libelle),
    pop_id = as.factor(pop_id),
    sta_id = as.factor(sta_id),
    # obj_libelle = as.factor(obj_libelle),
    indicateur = "effectif_total") %>% 
  # ) %>% # creation d'une colonne pour la période (ex : 2007-2008)
  # mutate(annee = {
  #   debut <- floor((annee - annee_debut_periode) / 2) * 2 + annee_debut_periode
  #   paste0(debut)
  # }, annee = as.numeric(annee)) %>% 
  mutate(julian = lubridate::yday(ope_date)) %>% #  Ajout de la date de pêche codée de 1 à 365
  select(
    ope_id,
    valeur,
    esp_code_alternatif,
    annee,
    ope_surface_calculee,
    pro_libelle,
    pop_id,
    sta_id,
    indicateur,
    x,
    y,
    ope_date, 
    julian, 
    # obj_libelle
  ) %>% 
  rename(espece = esp_code_alternatif)
```


```{r Mise en forme sortie fonction ZIP_GLMM}
zip_glmm_resultats_annee <- zip_glmm_resultats %>% 
  filter(row_name == "annee") %>% 
  select(periode,
         esp_code_alternatif,
         p_value,
         Estimate,
         sig, 
         family) %>% 
  mutate(trend = case_when(
      Estimate > 0 ~ "\U2197",
      Estimate < 0 ~ "\U2198",
      TRUE ~ "."))
```


```{r Sélection des données résultats ZIP_GLMM}
# Filtrer les données selon les critères spécifiés
zip_glmm_resultats_format <- zip_glmm_resultats_annee %>%
  filter((esp_code_alternatif == "ANG" & periode == "2007-2025") |
    (esp_code_alternatif %in% c(c("ABL","BAF","BLN","BRB", "BRE","BRO","CCO","CHA","CHE", "EPI", "EPT", "GAR", "GOU","HOT","LOF",   "LOR", "LOT","LPM" ,"LPP", "OBR", "PER", "ROT", "SAT", "SPI", "TAN", "TOX", "TRF","VAI","VAN","VAR")) & periode == "2007-2025"))

```

Construction d'un tableau récapitulatif des résultats du modèle GLMM :

```{r}
df_zip_glmm_resultats <- zip_glmm_resultats_format %>%
  select(esp_code_alternatif, periode, trend, sig, Estimate, family) %>%
  mutate(Estimate = round(Estimate, digits = 2)) %>%
  rename(
    "Code Espèce" = esp_code_alternatif,
    "Periode" = periode,
    "Tendance" = trend,
    "Coefficient" = Estimate,
    "Significativité" = sig,
    "Famille de distribution" = family
  ) %>%
  arrange(`Code Espèce`) # Ordonner par espèce, indicateur et stade

# Lignes positives et significatives
i_vert  <- which(
  df_zip_glmm_resultats$Tendance == "\U2197" &
    df_zip_glmm_resultats$Significativité %in% c("*", "**", "***")
)
# Lignes négatives et significatives
i_rouge <- which(
  df_zip_glmm_resultats$Tendance == "\U2198" &
    df_zip_glmm_resultats$Significativité %in% c("*", "**", "***")
)

tab_zip_glmm_resultats <- df_zip_glmm_resultats %>%
  flextable::flextable() %>%
  flextable::autofit() %>%
  flextable::colformat_int(j = c(1, 3), big.mark = " ") %>%
  flextable::theme_vanilla() %>%
  flextable::font(fontname = "calibri") %>%
  flextable::fontsize(size = 13) %>%
  flextable::bg(i = i_vert, bg = "darkolivegreen3") %>%   # vert clair
  flextable::bg(i = i_rouge, bg = "coral3") %>%   # rouge clair
  flextable::align(align = "center") %>%
  flextable::set_caption("Synthèse des tendances des effectifs des populations de poissons d'eau douce de BFC")

tab_zip_glmm_resultats
```