---
title: "LRR des poissons d'eau douce de BFC"
subtitle: "48_analyses_mixtes"
author: "PabloVigneron"
date: "`r format(Sys.time(), '%Y-%m-%d')`"
output: 
  bookdown::html_document2:
   fig_caption: yes
   tab_caption: yes
   number_sections: yes
   global_numbering: yes
   toc: yes
   toc_float:
         toc_collapsed: yes
         toc_depth: 2
   code_folding: hide
   css: "style.css"
#bibliography: .bib
#link-citations: true
#cls: apa.cls
#editor_options: 
#  markdown: 
#    wrap: sentence
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE,
                      message = FALSE,
                      warning = FALSE)
```

Script : 48_analyses_mixtes

  Ce script a pour objectif d'évaluer l'impact des différents réseaux sur les tendances des indicateurs régionaux. Il a également l'objectif de regarder l'impact des changements de protocoles sur les stations au cours des années d'études grâce aux zero inflated poisson GLMM. 


# Installation 

## Chargement des packages, fonctions et des données 

```{r Chargement des packages}
library(lme4)
library(aspe)
library(tidyverse)
library(aspe)
library(sjstats)
library(gt)
library(glmmTMB)
library(RVAideMemoire)
# library(lmerTest)  # remplace lme4 automatiquement
library(ggh4x)
library(mgcv) # modele GAMMs
library(gratia)
library(patchwork)
library(lme4)
library(car) # fonction Anova()
library(gstat)#semi_variogramme
library(DHARMa) # Ajusting of the model 
library(ggplotify)
library(ggpubr)

```

```{r Chargement des données}
load(file = "../processed_data/selection_especes.rda")
load(file = "../processed_data/selection_pop_ope.rda")
load(file = "../processed_data/donnees_densite_eff_abs.rda")
load(file = "../processed_data/assemblage_tab_par_ope.rda")
```

```{r Chargement des fonctions}
source(file = "../R/zip_glmm_calcul_modele.R")
source(file = "../R/zip_glmm_application_modele.R")
source(file = "../R/gamm_calcul_modele.R")
source(file = "../R/gamm_application_modele.R")
source(file = "../R/adequate_data_for_model.R")
```

```{r Chargement des tables de données}
rdata_tables <- misc_nom_dernier_fichier(
  repertoire = "../../../../Liste_rouge_BFC/raw_data",
  pattern = "^tables")
load(rdata_tables) # Chargement des données 
```

# Preparation du df 

```{r DF avec coord et absences}
# preparer la table pour jointure
# ope_selection <- ope_selection %>%
#   dplyr::select(ope_id, pop_id, sta_id)
# 
# # Ajout des coordonnées pour jointure avec df pour future analyse autocorrelation spatiale
# coord_sta_id <- point_prelevement %>%
#   filter(pop_id %in% pop_serie_tempo) %>%
#   rename(x = pop_coordonnees_x, y = pop_coordonnees_y) %>%
#   select(pop_id, x, y) %>%
#   mutate(pop_id = as.factor(pop_id))
# 
# # prepéarer la table pour jointure 
# esp_ope_selection_join <- esp_ope_selection %>% 
#   select(ope_id, ope_surface_calculee) %>% 
#   distinct(ope_id, .keep_all = TRUE)
# 
# 
# ope_effectif_glmm <- ope_effectif_absences %>%
#   mef_ajouter_type_protocole() %>%
#   left_join(ope_selection, by = "ope_id") %>%
#   inner_join(esp_ope_selection_join, by = "ope_id") %>%
#   mutate(
#     valeur = as.numeric(valeur) ,
#     annee = as.numeric(annee),
#     ope_surface_calculee = as.numeric(ope_surface_calculee),
#     pro_libelle = as.factor(pro_libelle),
#     pop_id = as.factor(pop_id),
#     sta_id = as.factor(sta_id)
#   ) %>%
#   left_join(coord_sta_id, by = "pop_id") %>% 
#   mutate(x = as.numeric(x), 
#          y = as.numeric(y))%>%
#   rename(espece = esp_code_alternatif)
```


```{r}
# preparer la table pour jointure
ope_selection <- ope_selection %>%
  dplyr::select(ope_id, pop_id, sta_id)

# Ajout des coordonnées pour jointure avec df pour future analyse autocorrelation spatiale
coord_sta_id <- point_prelevement %>%
  filter(pop_id %in% pop_serie_tempo) %>%
  rename(x = pop_coordonnees_x, y = pop_coordonnees_y) %>%
  select(pop_id, x, y) %>%
  mutate(pop_id = as.factor(pop_id))

# prepéarer la table pour jointure 
esp_ope_selection_join <- esp_ope_selection %>% 
  select(ope_id, ope_surface_calculee) %>% 
  distinct(ope_id, .keep_all = TRUE)


ope_effectif_glmm_abs <- ope_indicateur %>%
  filter(indicateur == "effectif_total") %>% 
  mef_ajouter_type_protocole() %>% 
  left_join( y= ope_selection, by = c("ope_id", "pop_id", "annee")) %>% 
  inner_join(esp_ope_selection_join, by = "ope_id") %>% 
  mutate(
    valeur = as.numeric(valeur) ,
    annee = as.numeric(annee),
    ope_surface_calculee = as.numeric(ope_surface_calculee),
    pro_libelle = as.factor(pro_libelle),
    pop_id = as.factor(pop_id),
    sta_id = as.factor(sta_id),
    stade = as.factor(stade)
  ) %>%
  left_join(coord_sta_id, by = "pop_id") %>%
  mutate(x = as.numeric(x), y = as.numeric(y)) %>%
  rename(espece = esp_code_alternatif) %>% 
  filter(stade == "ind")
```



# Effectif en fonction des annes

# Test ZIPGLMM Ajustement  avec absences 
```{r}
truite_data <- ope_effectif_glmm %>% 
  filter(espece == "TRF")
```

```{r}
# Modèle original
mod_zi_1.1 <- glmmTMB(
  valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 | sta_id),
  family = nbinom2(link = "log"),
  data   = truite_data
)

# Modèle avec terme quadratique
mod_zi_1.2 <- glmmTMB(
  valeur ~ annee + I(annee^2) + offset(log(ope_surface_calculee)) + pro_libelle + (1 | sta_id),
  family = nbinom2(link = "log"),
  data   = truite_data
)

# Modèle avec annee centree-reduite (recommande pour stabilite numerique)
truite_data <- truite_data %>%
  mutate(annee_scaled = scale(annee))

mod_zi_1.3 <- glmmTMB(
  valeur ~ annee_scaled + I(annee_scaled^2) + offset(log(ope_surface_calculee)) + pro_libelle + (1 | sta_id),
  family = nbinom2(link = "log"),
  data   = truite_data
)

mod_zi_1.4 <- glmmTMB(
  valeur ~ annee_scaled + I(annee_scaled^2) + offset(log(ope_surface_calculee)) + pro_libelle + (1 | sta_id),
  family = poisson(link = "log"),
  data   = truite_data
)

mod_zi_1.5 <- glmmTMB(
  valeur ~ annee_scaled + I(annee_scaled^2) + offset(log(ope_surface_calculee)) + pro_libelle + (1 | sta_id),
  family = nbinom2(link = "log"),
  data   = truite_data
)

mod_zi_1.6 <- glmmTMB(
  valeur ~ annee_scaled + I(annee_scaled^2) + offset(log(ope_surface_calculee)) + pro_libelle + (1 | sta_id),
  family = truncated_poisson(link = "log"),
    ziformula = ~1,
  data   = truite_data
)

mod_zi_1.7 <- glmmTMB(
  valeur ~ annee_scaled + I(annee_scaled^2) + offset(log(ope_surface_calculee)) + pro_libelle + (1 | sta_id),
  family = truncated_nbinom2(link = "log"),
    ziformula = ~1,
  data   = truite_data
)
# Comparer les modèles
AIC(mod_zi_1.1, mod_zi_1.2, mod_zi_1.3, mod_zi_1.4, mod_zi_1.5, mod_zi_1.6, mod_zi_1.7)

# Relancer DHARMa sur le meilleur
library(DHARMa)
simulationOutput_1 <- simulateResiduals(fittedModel = mod_zi_1.1, plot = TRUE, )
simulationOutput_2 <- simulateResiduals(fittedModel = mod_zi_1.2, plot = TRUE)
simulationOutput_3 <- simulateResiduals(fittedModel = mod_zi_1.3, plot = TRUE)

plotResiduals(mod_zi_1.1, quantreg = F)
plotResiduals(mod_zi_1.2, quantreg = F)
plotResiduals(mod_zi_1.3, quantreg = F)

plotresid(mod_zi_1.1)
plotresid(mod_zi_1.2)
plotresid(mod_zi_1.3)

```

#Test ZIPGLMM sans absence 


```{r DF avec coord sans absences}
# preparer la table pour jointure
ope_selection <- ope_selection %>%
  dplyr::select(ope_id, pop_id, sta_id)

# Ajout des coordonnées pour jointure avec df pour future analyse autocorrelation spatiale
coord_sta_id <- point_prelevement %>%
  filter(pop_id %in% pop_serie_tempo) %>%
  rename(x = pop_coordonnees_x, y = pop_coordonnees_y) %>%
  select(pop_id, x, y) %>%
  mutate(pop_id = as.factor(pop_id))

# prepéarer la table pour jointure 
esp_ope_selection_join <- esp_ope_selection %>% 
  select(ope_id, ope_surface_calculee) %>% 
  distinct(ope_id, .keep_all = TRUE)


ope_effectif_glmm_sa <- ope_effectif %>%
  mef_ajouter_type_protocole() %>%
  left_join(ope_selection, by = "ope_id") %>%
  inner_join(esp_ope_selection_join, by = "ope_id") %>%
  mutate(
    valeur = as.numeric(valeur) ,
    annee = as.numeric(annee),
    ope_surface_calculee = as.numeric(ope_surface_calculee),
    pro_libelle = as.factor(pro_libelle),
    pop_id = as.factor(pop_id),
    sta_id = as.factor(sta_id)
  ) %>%
  left_join(coord_sta_id, by = "pop_id") %>% 
  mutate(x = as.numeric(x), 
         y = as.numeric(y))%>%
  rename(espece = esp_code_alternatif)
```

```{r}
truite_data <- ope_effectif_glmm_sa %>% 
  filter(espece == "TRF")
```

```{r}
# Modèle original
mod_zi_1.1 <- glmmTMB(
  valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 | sta_id),
  family = nbinom2(link = "log"),
  data   = truite_data
)

# Modèle avec terme quadratique
mod_zi_1.2 <- glmmTMB(
  valeur ~ annee + I(annee^2) + offset(log(ope_surface_calculee)) + pro_libelle + (1 | sta_id),
  family = nbinom2(link = "log"),
  data   = truite_data
)

# Modèle avec annee centree-reduite (recommande pour stabilite numerique)
truite_data <- truite_data %>%
  mutate(annee_scaled = scale(annee))

mod_zi_1.3 <- glmmTMB(
  valeur ~ annee_scaled + I(annee_scaled^2) + offset(log(ope_surface_calculee)) + pro_libelle + (1 | sta_id),
  family = nbinom2(link = "log"),
  data   = truite_data
)

mod_zi_1.4 <- glmmTMB(
  valeur ~ annee_scaled + I(annee_scaled^2) + offset(log(ope_surface_calculee)) + pro_libelle + (1 | sta_id),
  family = poisson(link = "log"),
  data   = truite_data
)

mod_zi_1.5 <- glmmTMB(
  valeur ~ annee_scaled + I(annee_scaled^2) + offset(log(ope_surface_calculee)) + pro_libelle + (1 | sta_id),
  family = nbinom2(link = "log"),
  data   = truite_data
)

mod_zi_1.6 <- glmmTMB(
  valeur ~ annee_scaled + I(annee_scaled^2) + offset(log(ope_surface_calculee)) + pro_libelle + (1 | sta_id),
  family = truncated_poisson(link = "log"),
    ziformula = ~1,
  data   = truite_data
)

mod_zi_1.7 <- glmmTMB(
  valeur ~ annee_scaled + I(annee_scaled^2) + offset(log(ope_surface_calculee)) + pro_libelle + (1 | sta_id),
  family = truncated_nbinom2(link = "log"),
    ziformula = ~1,
  data   = truite_data
)
mod_zi_1.8 <- glmmTMB(
  valeur ~ annee_scaled + offset(log(ope_surface_calculee)) + pro_libelle + (1 | sta_id),
  family = truncated_nbinom2(link = "log"),
    ziformula = ~1,
  data   = truite_data
)
# Comparer les modèles
AIC(mod_zi_1.1, mod_zi_1.2, mod_zi_1.3, mod_zi_1.4, mod_zi_1.5, mod_zi_1.6, mod_zi_1.7, mod_zi_1.8)

# Relancer DHARMa sur le meilleur
library(DHARMa)
simulationOutput_1 <- simulateResiduals(fittedModel = mod_zi_1.1, plot = TRUE )
simulationOutput_2 <- simulateResiduals(fittedModel = mod_zi_1.2, plot = TRUE)
simulationOutput_3 <- simulateResiduals(fittedModel = mod_zi_1.3, plot = TRUE)

plotResiduals(mod_zi_1.1, quantreg = F)
plotResiduals(mod_zi_1.2, quantreg = F)
plotResiduals(mod_zi_1.3, quantreg = F)
plotResiduals(mod_zi_1.4, quantreg = F)
plotResiduals(mod_zi_1.5, quantreg = F)
plotResiduals(mod_zi_1.6, quantreg = F)
plotResiduals(mod_zi_1.7, quantreg = F)
plotResiduals(mod_zi_1.8, quantreg = F)

plotresid(mod_zi_1.1)
plotresid(mod_zi_1.2)
plotresid(mod_zi_1.3)
plotresid(mod_zi_1.4)
plotresid(mod_zi_1.5)
plotresid(mod_zi_1.6)
plotresid(mod_zi_1.7)
plotresid(mod_zi_1.8)

```

# test de prédiction 
```{r}
# tableau <- truite_data %>% 
#   select(
#     annee, 
#     ope_surface_calculee,
#     pro_libelle, 
#     annee, 
#     sta_id
#   )
```

```{r prediction des modeles}
 # predict(mod_zi_1.1,newdata=tableau,type="response", re.form =NA)
```

```{r}

# x <- RVAideMemoire::seq2(truite_data$annee)
# 
# plot(truite_data$valeur ~ truite_data$annee)
# 
# lines(x,
#       predict(
#         mod_zi_1.1,
#         newdata = list(
#           annee = x,
#           offse = 1, 
#           sta_id = 163,
#           pro_libelle = c("Pêche complète à un ou plusieurs passages", length(x))
#         ),
#         type = "response"
#       ),
#       re.form = NA)


```



```{r}
# plot(truite_data$valeur ~ truite_data$pro_libelle)
```




```{r}
summary(mod_zi_1.1)
summary(mod_zi_1.2)
summary(mod_zi_1.3)
summary(mod_zi_1.4)
summary(mod_zi_1.5)
summary(mod_zi_1.6)
summary(mod_zi_1.7)
summary(mod_zi_1.8)

```


# Test GAMM negative binomial

```{r}
truite_data_sa <- ope_effectif_glmm_sa %>% 
  filter(espece == "TRF")
```


```{r}
# Avec ajout des absences
mod_gamm_1_sa <- gamm(
  valeur ~ s(annee) + offset(log(ope_surface_calculee)) + pro_libelle + s(x,y),
  random = list(sta_id =  ~ 1),
  data = truite_data,
  family = poisson(link = "log"),
  method = "REML"
)

mod_gamm_2_sa <- gamm(
  valeur ~ s(annee) + offset(log(ope_surface_calculee)) + pro_libelle + s(x,y),
  random = list(sta_id =  ~ 1),
  data = truite_data,
  family = poisson(link = "sqrt"),
  method = "REML"
)

mod_gamm_3_sa <- gam(
  valeur ~ s(annee) + offset(log(ope_surface_calculee)) + pro_libelle + s(x,y) + s(sta_id, bs = "re"), 
  data = truite_data,
  family = poisson(link = "log"),
  method = "REML"
)

mod_gamm_4_sa <- gam(
  valeur ~ s(annee) + offset(log(ope_surface_calculee)) + pro_libelle + s(x,y) + s(sta_id, bs = "re"), 
  data = truite_data,
  family = poisson(link = "sqrt"),
  method = "REML"
)


################################################
#QP
mod_gamm <- gam(
  valeur ~ s(annee) + offset(log(ope_surface_calculee)) + pro_libelle + s(x, y) + s(sta_id, bs = "re"),
  data = truite_data,
  family = quasipoisson(link = "log"),
  method = "REML")

# NB GAMM4
mod_gamm_5 <- gamm4(
  valeur ~ s(annee) + offset(log(ope_surface_calculee)) + pro_libelle + s(x, y),
  random = ~ (1 | sta_id),
  data = truite_data,
  family = nb(link = "log"),
  method = "REML"
)

# Poisson GAM 
mod_gamm_7 <- gam(
  valeur ~ s(annee) + offset(log(ope_surface_calculee)) + pro_libelle + s(x, y) + s(sta_id, bs = "re"),
  data = truite_data,
  family = poisson(link = "log"),
  method = "REML"
)
# Poisson GAMM4
mod_gamm_8 <- gamm4(
  valeur ~ s(annee) + offset(log(ope_surface_calculee)) + pro_libelle + s(x, y),
  random = ~ (1 | sta_id),
  data = truite_data,
  family = poisson(link = "log"),
  method = "REML"
)

######################
```

```{r}
mod_gamm <- gam(
  valeur ~ s(annee) + offset(log(ope_surface_calculee)) + pro_libelle  + s(sta_id, bs = "re"),
  data = truite_data,
  family = nb(link = "log"),
  method = "REML"
)
# NB GAM 
mod_gamm_5 <- gam(
  valeur ~ s(annee, k = 15) + offset(log(ope_surface_calculee)) + pro_libelle + s(x, y) + s(sta_id, bs = "re"),
  data = truite_data,
  family = nb(link = "log"),
  method = "REML"
)
mod_gamm_6 <- gam(
  valeur ~ s(annee) + offset(log(ope_surface_calculee)) + pro_libelle + s(x, y) + s(sta_id, bs = "re"),
  data = truite_data,
  family = poisson(link = "log"),
  method = "REML"
)
# mod_gamm_7 <- gam(
#   valeur ~ s(annee) + offset(log(ope_surface_calculee)) + pro_libelle + s(x, y) + s(sta_id, bs = "re"),
#   data = truite_data,
#   family = poisson(link = "identity"),
#   method = "REML"
# )
mod_gamm_8 <- gam(
  valeur ~ s(annee) + offset(log(ope_surface_calculee)) + pro_libelle + s(x, y) + s(sta_id, bs = "re"),
  data = truite_data,
  family = quasipoisson(link = "log"),
  method = "REML"
)
```

```{r}
appraise(mod_gamm)
appraise(mod_gamm_5)
appraise(mod_gamm_6)
# appraise(mod_gamm_7)
appraise(mod_gamm_8)

```
```{r}
draw(mod_gamm, residuals = T, scales = "free", shade = T, select = "s(annee)")

draw(mod_gamm_5, residuals = T, scales = "free", shade = T, select = "s(annee)")
draw(mod_gamm_6, residuals = T, scales = "fixed", shade = T,  select = "s(annee)")
# draw(mod_gamm_7, residuals = T, scales = "fixed", shade = T, select = "s(annee)")
draw(mod_gamm_8, residuals = T, scales = "fixed", shade = T, select = "s(annee)")

```
```{r}
k.check(mod_gamm)
k.check(mod_gamm_5)
k.check(mod_gamm_6)
# k.check(mod_gamm_7)
k.check(mod_gamm_8)
```
```{r}
AIC(
  mod_gamm,
  mod_gamm_5,
  mod_gamm_6,
  # mod_gamm_7,
  mod_gamm_8
  )

```

```{r}
var <- variogram(valeur ~ annee + offset(log(ope_surface_calculee)), locations = ~ x+y, data = truite_data_abs)
var
plot(var, col= "black")
```


```{r}
# DHARMa::testDispersion(mod_gamm)
DHARMa::testDispersion(mod_gamm_5)
DHARMa::testDispersion(mod_gamm_6)
# DHARMa::testDispersion(mod_gamm_7)
DHARMa::testDispersion(mod_gamm_8)

```

```{r}
# simulateResiduals(fittedModel = mod_gamm, plot = TRUE, n = 1000)
simulateResiduals(fittedModel = mod_gamm_5, plot = TRUE, n = 1000)
simulateResiduals(fittedModel = mod_gamm_6, plot = TRUE, n = 1000)
# simulateResiduals(fittedModel = mod_gamm_7, plot = TRUE, n = 1000)
simulateResiduals(fittedModel = mod_gamm_8, plot = TRUE, n = 1000)

```

```{r}
acf(residuals(mod_gamm_5, type = "normalized"), lag.max = 20)
acf(residuals(mod_gamm_8$mer, type = "normalized"), lag.max = 20)
acf(residuals(mod_gamm_7, type = "normalized"), lag.max = 20)
```




```{r}
draw(gratia::derivatives(mod_gamm, select = "s(annee)", interval = "confidence")) +
  geom_hline(
    yintercept = 0,
    color = "firebrick",
    linetype = "dashed",
    linewidth = 0.5
  ) 
draw(gratia::derivatives(mod_gamm_5, select = "s(annee)", interval = "confidence")) +
  geom_hline(
    yintercept = 0,
    color = "firebrick",
    linetype = "dashed",
    linewidth = 0.5
  ) 

# draw(gratia::derivatives(mod_gamm_6, select = "s(annee)", interval = "confidence")) +
#   geom_hline(
#     yintercept = 0,
#     color = "firebrick",
#     linetype = "dashed",
#     linewidth = 0.5
#   ) 

draw(gratia::derivatives(mod_gamm_7, select = "s(annee)", interval = "confidence")) +
  geom_hline(
    yintercept = 0,
    color = "firebrick",
    linetype = "dashed",
    linewidth = 0.5
  ) 

draw(gratia::derivatives(mod_gamm_8$gam, select = "s(annee)", interval = "confidence")) +
  geom_hline(
    yintercept = 0,
    color = "firebrick",
    linetype = "dashed",
    linewidth = 0.5
  ) 
```
```{r}
summary(mod_gamm_5)
```


### GAMM brouillon

```{r}
# appraise(mod_gamm_1_sa$gam)
# appraise(mod_gamm_2_sa$gam)
# 
# appraise(mod_gamm_3_sa)
# # appraise(mod_gamm_4_sa)
# appraise(mod_gamm_5_sa)
# 
# draw(mod_gamm_1_sa$gam, residuals = T, scales = "fixed", shade = T)
# draw(mod_gamm_2_sa$gam, residuals = T, scales = "fixed", shade = T)
# 
# draw(mod_gamm_3_sa, residuals = T, scales = "fixed", shade = T)
# draw(mod_gamm_5_sa, residuals = T, scales = "fixed", shade = T)

```
```{r}
AIC(mod_gamm_1_sa$lme, mod_gamm_2_sa$lme, mod_gamm_3_sa, mod_gamm_5_sa, mod_gamm_6_sa )
```

```{r}
# install.packages("gamm4")
```

```{r}
library(gamm4)
# mod_gamm_5_sa <- gam(
#   valeur ~ s(annee) + offset(log(ope_surface_calculee)) + pro_libelle + s(x,y) + s(sta_id, bs = "re"), 
#   data = truite_data,
#   family = nb(link = "log"),
#   method = "REML"
# )
# gamm4(
#   valeur ~ s(annee) + offset(log(ope_surface_calculee)) + pro_libelle + s(x, y),
#   random = ~ (1 | sta_id),
#   data = truite_data,
#   family = nb(link = "log"),
#   method = "REML"
# )
```


```{r}
gam.check(mod_gamm_2_sa$gam)


gam.check(mod_gamm_3_sa)

gam.check(mod_gamm_5_sa)
gam.check(mod_gamm_6_sa)


```

```{r}
summary(mod_gamm_1_sa$gam)
summary(mod_gamm_2_sa$gam)
summary(mod_gamm_3_sa)
summary(mod_gamm_4_sa)
summary(mod_gamm_5_sa)

```
```{r}
# installed.packages("mgcViz")
# library(mgcViz)
DHARMa::plotResiduals(simulateResiduals(fittedModel = mod_gamm_5_sa, plot = TRUE, n = 1000))
DHARMa::plotResiduals(simulateResiduals(fittedModel = mod_gamm_6_sa, plot = TRUE, n = 1000), quantreg = T)
DHARMa::plotResiduals(simulateResiduals(fittedModel = mod_gamm_3_sa, plot = TRUE, n = 1000))

```

```{r}
DHARMa::testDispersion(mod_gamm_5_sa)
```

```{r, fig.height = 7, fig.width = 12}
draw(mod_gamm_5_sa, residuals = T, scales = "fixed", shade = T,  overall_uncertainty = TRUE, select = "s(annee)")

```

```{r}
d <- gratia::derivatives(mod_gamm_5_sa, select = "s(annee)", interval = "confidence")
draw(d)+
    geom_hline(yintercept = 0, color = "firebrick", linetype = "dashed", linewidth = 0.5) 

```

```{r}
# nd <- data.frame(annee = c(min(truite_data$annee), 
#                             max(truite_data$annee)),
#                   ope_surface_calculee = 1,  # neutralise l'offset
#                   pro_libelle = "Pêche complète à un ou plusieurs passages")
# 
# pred <- predict(mod_gamm_5_sa, newdata = nd, type = "lpmatrix")
# diff <- pred[2, ] - pred[1, ]
# est  <- diff %*% coef(mod_gamm_5_sa)
# se   <- sqrt(t(diff) %*% vcov(mod_gamm_5_sa) %*% diff)
# c(estimate = est, lower = est - 1.96*se, upper = est + 1.96*se)
```

```{r}
# install.packages("marginaleffects")
library(marginaleffects)
```

```{r}
marginaleffects::plot_predictions(mod_gamm_5_sa, by  = 'annee', 
                 type = 'response', points = 0.5,
                 rug = FALSE, gray = T) +
  labs(y = "Expected response",
       title = "Average smooth effect of concentration",
       subtitle = "Aggregated across treatments and types")
```

```{r}
AIC(mod_gamm_1_sa$lme, mod_gamm_2_sa$lme, mod_gamm_1$lme, mod_gamm_2$lme)
```

```{r}
appraise(mod_gamm_1_sa$gam)
appraise(mod_gamm_2_sa$gam)
appraise(mod_gamm_1$gam)
appraise(mod_gamm_2$gam)

```





```{r}
k.check(mod_gamm_1_sa$gam)
```


```{r, fig.height = 10, fig.width = 20}
draw(mod_gamm_1_sa, residuals = T)
draw(mod_gamm_2_sa, residuals = T)
draw(mod_gamm_1, residuals = T)
draw(mod_gamm_2, residuals = T)
```



```{r}
summary(mod_gamm_2$gam)
```

```{r}
summary(mod_gamm_2_sa$gam)
```




# test GAMM avec absences
```{r}
mod_gamm_1.0 <- gamm(
  valeur ~ s(annee) + offset(log(ope_surface_calculee)) + pro_libelle,
  random = list(sta_id =  ~ 1),
  data = truite_data,
  family = poisson(link = "log"),
  method = "REML"
)
mod_gamm_1.1 <- gamm(
  valeur ~ s(annee) + offset(log(ope_surface_calculee))  + pro_libelle,
  random = list(sta_id =  ~ 1),
  data = truite_data,
  family = poisson(link = "identity"),
  method = "REML"
)
mod_gamm_1.2 <- gamm(
  valeur ~ s(annee) + offset(log(ope_surface_calculee)) + pro_libelle,
  random = list(sta_id =  ~ 1),
  data = truite_data,
  family = poisson(link = "sqrt"),
  method = "REML"
)

mod_gamm_1.3 <- gamm(
  valeur ~ s(annee) + offset(log(ope_surface_calculee)) + pro_libelle + s(x, y),
  random = list(sta_id =  ~ 1),
  data = truite_data,
  family = poisson(link = "log"),
  method = "REML"
)
mod_gamm_1.4 <- gamm(
  valeur ~ s(annee) + offset(log(ope_surface_calculee)) + pro_libelle + s(x, y),
  random = list(sta_id =  ~ 1),
  data = truite_data,
  family = poisson(link = "sqrt"),
  method = "REML"
)
```
```{r}
AIC(mod_gamm_1.0$lme,mod_gamm_1.1$lme, mod_gamm_1.2$lme,mod_gamm_1.3$lme,mod_gamm_1.4$lme)
```
### Autocorelation temporelle - plot

```{r autocorrelation spatiale plot, fig.height = 15, fig.width = 24}
acf_mod_gamm_1.0 <- as.ggplot(~ acf(residuals(mod_gamm_1.0$lme, type = "normalized"), lag.max = 20))
acf_mod_gamm_1.1 <- as.ggplot(~ acf(residuals(mod_gamm_1.1$lme, type = "normalized"), lag.max = 20))
acf_mod_gamm_1.2 <- as.ggplot(~ acf(residuals(mod_gamm_1.2$lme, type = "normalized"), lag.max = 20))
acf_mod_gamm_1.3 <- as.ggplot(~ acf(residuals(mod_gamm_1.3$lme, type = "normalized"), lag.max = 20))

ggarrange(
  acf_mod_gamm_1.0,
  acf_mod_gamm_1.1,
  acf_mod_gamm_1.2,
  acf_mod_gamm_1.3,
  ncol = 3,
  nrow = 2,
  labels = c("acf_mod_gamm_1.0", "acf_mod_gamm_1.1", "acf_mod_gamm_1.2", "acf_mod_gamm_1.3"),
  font.label = list(
    size = 14,
    color = "darkred",
    face = "bold",
    family = NULL
  ),
  hjust = -0.1,
  vjust = 1.6,
  align = c("none", "h", "v", "hv")
)
```


### Autocorelation spatial - semi-variogramme

```{r, semivariogramme pour tester autocorrelation spatiale avec uniquement la variable reponse}
var <- variogram(valeur ~ annee + offset(log(ope_surface_calculee)), locations = ~ x+y, data = truite_data)
var
plot(var, col= "black")
```

```{r}
show.vgms(models = c("Exp",  # Exponential model
                     "Sph",  # Spherical model
                     "Gau",  # Gaussian model
                     "Nug"), # Nugget model
          nugget = 50)
```

```{r}
vfit <- fit.variogram(var, vgm(c("Exp", "Gau", "Sph")))
vfit
```

```{r}
# Did the model-fitting process converge successfully?
# This is not guaranteed for nonlinear models.
attributes(vfit)$singular
```

```{r}
# For convenience, use show.vgms() to calculate the
# predicted values for our fitted model
fitted <-show.vgms(min = min(var$dist), 
                   max = max(var$dist), 
                   models ="Sph",
                   range = vfit$range[2],
                   sill = vfit$psill[2],
                   nugget = vfit$psill[1],
                   plot = FALSE)

# Now we can easily plot this against the observed data
ggplot(var, aes(dist, gamma)) + 
  geom_point() + 
  geom_line(data = fitted, 
            aes(distance, semivariance),
            linetype = 2)
```


### Indépendance du modèles aux data

```{r plots résidus}
appraise(mod_gamm_1.0$gam)
appraise(mod_gamm_1.1$gam)
appraise(mod_gamm_1.2$gam)
appraise(mod_gamm_1.3$gam)
appraise(mod_gamm_1.4$gam)

```

```{r}
k.check(mod_gamm_1.0$gam)
k.check(mod_gamm_1.1$gam)
k.check(mod_gamm_1.2$gam)
k.check(mod_gamm_1.3$gam)
k.check(mod_gamm_1.4$gam)

```


### Verifier la concurvity

```{r}
concurvity(mod_gamm_1.0$gam)
concurvity(mod_gamm_1.1$gam)
concurvity(mod_gamm_1.2$gam)
concurvity(mod_gamm_1.3$gam)
concurvity(mod_gamm_1.4$gam)

```

### Représentation graphique des smooth

```{r representation graphique standard, fig.height = 10, fig.width = 20}
draw(mod_gamm_1.0, residuals = T)
draw(mod_gamm_1.1, residuals = T)
draw(mod_gamm_1.2, residuals = T)
draw(mod_gamm_1.3, residuals = T)
draw(mod_gamm_1.4, residuals = T)

```


```{r}
AIC(mod_gamm_1.0$lme,mod_gamm_1.1$lme, mod_gamm_1.2$lme,mod_gamm_1.3$lme )
```
```{r}
summary(mod_gamm_1.0$gam)
summary(mod_gamm_1.1$gam)
summary(mod_gamm_1.2$gam)
summary(mod_gamm_1.3$gam)
summary(mod_gamm_1.4$gam)
```

```{r}
coefficients(mod_gamm_1.4$gam)
```



## Test gamm function
```{r}
liste_periodes_etude <- list(c(2007, 2025)) # Liste des périodes (chaque période est un vecteur avec l'année de début et l'année de fin) 
```

### Application du modèle GLMM avec les effectifs


```{r}
source(file = "../R/gamm_poptrend.R")
source(file = "../R/gamm_poptrend_application.R")

data_esp <- ope_effectif_glmm_abs %>% 
  filter(espece == c("TRF"))


gamm_poptrend_application(data = ope_effectif_glmm_abs, liste_periodes = liste_periodes_etude)

```


## Modèles Linéaires Généralisé Mixtes (ZIGLMM) de zéro enflées
```{r}
data_esp <- ope_effectif_glmm_abs %>% 
  filter(espece == "TRF")

mod_zi_1.2 <- glmmTMB(
  valeur ~ annee + offset(log(ope_surface_calculee)) + pro_libelle + (1 | sta_id) ,
  family = truncated_poisson(link = "log"),
  data = data_esp,
  ziformula = ~ 1
)

source(file = "../R/gamm_poptrend.R")

gamm_poptrend(data = data_esp, mon_espec = "TRF")
  
trend <- poptrend::ptrend(
  valeur ~ trend(annee, tempRE = F, type = "smooth") + offset(log(ope_surface_calculee)) + pro_libelle + s(x, y) + s(sta_id, bs = "re"),
  data = data_esp,
  family = nb(link = "log"),
  bootType = "hessian",
  gamModel = TRUE,
  engine = "gam"
)

appraise(trend$gam)+labs(caption = mon_espece)

plot(trend, main = "TRF")
```

```{r}
trend <- poptrend::ptrend(
  valeur ~ trend(annee, tempRE = F, type = "smooth") + offset(log(ope_surface_calculee)) + pro_libelle + s(x, y) + s(sta_id, bs = "re"),
  data = data_esp,
  family = nb(link = "log"),
  bootType = "hessian",
  gamModel = TRUE,
  engine = "gam"
)
library(mgcv)
library(ggplot2)

mod <- trend$gam  # le gam sous-jacent

# séquence d'années sur laquelle prédire
newdat <- data.frame(
  annee = seq(min(data_esp$annee), max(data_esp$annee), length.out = 200),
  ope_surface_calculee = 1,          # offset neutralisé (log(1) = 0)
  pro_libelle = levels(data_esp$pro_libelle)[1],  # niveau de référence
  x = mean(data_esp$x),
  y = mean(data_esp$y),
  sta_id = data_esp$sta_id[1]        # niveau arbitraire, exclu ensuite
)

pred <- predict(mod, newdata = newdat, type = "terms", se.fit = TRUE,
                 exclude = c("s(x,y)", "s(sta_id)"))  # adapte les noms de smooth

# récupérer uniquement le terme lié à l'année (le nom exact dépend du modèle,
# vérifie avec : colnames(pred$fit))
term_name <- grep("annee", colnames(pred$fit), value = TRUE)

df_plot <- data.frame(
  annee = newdat$annee,
  fit   = pred$fit[, term_name],
  se    = pred$se.fit[, term_name]
)

df_plot$lower <- exp(df_plot$fit - 1.96 * df_plot$se)
df_plot$upper <- exp(df_plot$fit + 1.96 * df_plot$se)
df_plot$index <- exp(df_plot$fit)

ggplot(df_plot, aes(x = annee, y = index)) +
  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = "darkorange") +
  geom_line(color = "darkorange", linewidth = 1) +
  labs(x = "Année", y = "Indice relatif", title = "Tendance estimée (GAM)") +
  theme_minimal()

str(trend, max.level = 1)
names(trend)
class(trend)

head(trend$trendFrame)
str(trend$trendFrame)
colnames(trend$trendFrame)

library(ggplot2)
library(dplyr)

# 1. Isoler les points de la grille de prédiction (courbe lisse)
grid_idx <- which(trend$trendFrame$isGridP)

df_grid <- trend$trendFrame[grid_idx, c("annee", "trend")] %>%
  arrange(annee)

# 2. Intervalle de confiance à partir des réplicats bootstrap alignés
boot_grid <- trend$bootTrend[grid_idx, ]

ci <- t(apply(boot_grid, 1, quantile, probs = c(0.025, 0.975), na.rm = TRUE))
df_grid$lower <- ci[, 1]
df_grid$upper <- ci[, 2]

# 3. Graphique ggplot2
ggplot(df_grid, aes(x = annee, y = trend)) +
  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = "steelblue") +
  geom_line(color = "steelblue", linewidth = 1) +
  labs(
    x = "Année",
    y = "Indice de tendance",
    title = "Tendance de population estimée"
  ) +
  theme_minimal()

plot(trend)

library(ggplot2)
library(dplyr)

# 1. Isoler les points de la grille de prédiction (courbe lisse)
grid_idx <- which(trend$trendFrame$isGridP)

df_grid <- trend$trendFrame[grid_idx, c("annee", "trend")] %>%
  arrange(annee)

# 2. IC à partir des réplicats bootstrap alignés
boot_grid <- trend$bootTrend[grid_idx, ]
ci <- t(apply(boot_grid, 1, quantile, probs = c(0.025, 0.975), na.rm = TRUE))
df_grid$lower <- ci[, 1]
df_grid$upper <- ci[, 2]

# 3. Catégoriser la significativité par rapport à la référence (indice = 1)
df_grid <- df_grid %>%
  mutate(
    signif = case_when(
      lower > 1 ~ "Augmentation significative",
      upper < 1 ~ "Diminution significative",
      TRUE      ~ "Non significatif"
    )
  )

# 4. Pour colorer un TRAIT CONTINU par segments (et non des points isolés),
#    on découpe la courbe en segments consécutifs entre chaque paire de points
df_seg <- df_grid %>%
  mutate(
    xend = lead(annee),
    yend = lead(trend),
    signif_seg = signif  # on garde la catégorie du point de départ du segment
  ) %>%
  filter(!is.na(xend))

# 5. Graphique
ggplot() +
  geom_ribbon(data = df_grid, aes(x = annee, ymin = lower, ymax = upper),
              alpha = 0.15, fill = "grey50") +
  geom_segment(data = df_seg,
               aes(x = annee, y = trend, xend = xend, yend = yend, color = signif_seg),
               linewidth = 1.2) +
  geom_hline(yintercept = 1, linetype = "dashed", color = "grey40") +
  scale_color_manual(
    name = "Tendance",
    values = c(
      "Augmentation significative" = "forestgreen",
      "Diminution significative"   = "darkorange",
      "Non significatif"           = "grey30"
    )
  ) +
  labs(
    x = "Année",
    y = "Indice de tendance",
    title = "Tendance de population estimée"
  ) +
  theme_minimal() +
  theme(legend.position = "bottom")

range(df_grid$trend)
summary(trend$trendFrame$trend[trend$trendFrame$isGridP])
library(ggplot2)
library(dplyr)

# 1. Isoler les points de la grille de prédiction (courbe lisse)
grid_idx <- which(trend$trendFrame$isGridP)

df_grid <- trend$trendFrame[grid_idx, c("annee", "trend")] %>%
  arrange(annee)

# 2. IC à partir des réplicats bootstrap alignés
boot_grid <- trend$bootTrend[grid_idx, ]
ci <- t(apply(boot_grid, 1, quantile, probs = c(0.025, 0.975), na.rm = TRUE))
df_grid$lower <- ci[, 1]
df_grid$upper <- ci[, 2]

# 3. Catégoriser la significativité par rapport à la référence (indice = 1)
df_grid <- df_grid %>%
  mutate(
    signif = case_when(
      lower > 1 ~ "Augmentation significative",
      upper < 1 ~ "Diminution significative",
      TRUE      ~ "Non significatif"
    )
  )

# 4. Découper la courbe en segments consécutifs pour pouvoir la colorer par tronçons
df_seg <- df_grid %>%
  mutate(
    xend = lead(annee),
    yend = lead(trend),
    signif_seg = signif
  ) %>%
  filter(!is.na(xend))

# 5. Graphique final
ggplot() +
  geom_ribbon(data = df_grid, aes(x = annee, ymin = lower, ymax = upper),
              alpha = 0.15, fill = "grey50") +
  geom_segment(data = df_seg,
               aes(x = annee, y = trend, xend = xend, yend = yend, color = signif_seg),
               linewidth = 1.2) +
  geom_hline(yintercept = 1, linetype = "dashed", color = "grey40") +
  scale_color_manual(
    name = "Tendance",
    values = c(
      "Augmentation significative" = "forestgreen",
      "Diminution significative"   = "darkorange",
      "Non significatif"           = "grey30"
    )
  ) +
  labs(
    x = "Année",
    y = "Indice de tendance",
    title = "Tendance de population estimée"
  ) +
  theme_minimal() +
  theme(legend.position = "bottom")
```





```{r}
source(file = "../R/zip_glmm_calcul_modele.R")
source(file = "../R/zip_glmm_application_modele.R")
```

```{r}
liste_periodes_etude <- list(c(2007, 2025)) # Liste des périodes (chaque période est un vecteur avec l'année de début et l'année de fin) 
```

### Application du modèle GLMM avec les effectifs

```{r Exécution fonction ZIP_GLMM}
zip_glmm_resultats <- zip_glmm_application_modele(data = ope_effectif_glmm_abs,
                                          liste_periodes = liste_periodes_etude)
```


```{r Mise en forme sortie fonction ZIP_GLMM}
zip_glmm_resultats_annee <- zip_glmm_resultats %>% 
  filter(row_name == "annee") %>% 
  separate_wider_delim(periode, delim = "-", 
                        names = c("annee_debut", "annee_fin"), 
                        cols_remove = FALSE) %>%  # garde la colonne periode originale
  mutate(
    annee_debut = as.numeric(annee_debut),
    annee_fin   = as.numeric(annee_fin),
    nb_annees   = annee_fin - annee_debut
  ) %>% 
  select(periode, esp_code_alternatif, p_value, Estimate, sig, family, nb_annees) %>% 
  mutate(
    trend = case_when(
      Estimate > 0 ~ "\U2197",
      Estimate < 0 ~ "\U2198",
      TRUE ~ "."
    ),    # Ajouter le calcul de taux de changement dans la population
    rate_change = (exp(Estimate * nb_annees) - 1) * 100
)
```


```{r Sélection des données résultats ZIP_GLMM}
# Filtrer les données selon les critères spécifiés
zip_glmm_resultats_format <- zip_glmm_resultats_annee %>%
  filter((esp_code_alternatif == "ANG" & periode == "2007-2025") |
    (esp_code_alternatif %in% c(c("ABL","BAF","BLN","BRB", "BRE","BRO","CCO","CHA","CHE", "EPI", "EPT", "GAR", "GOU","HOT","LOF",   "LOR", "LOT","LPM" ,"LPP", "OBR", "PER", "ROT", "SAT", "SPI", "TAN", "TOX", "TRF","VAI","VAN","VAR")) & periode == "2007-2025"))

```

Construction d'un tableau récapitulatif des résultats du modèle GLMM :

```{r}
df_zip_glmm_resultats <- zip_glmm_resultats_format %>%
  select(esp_code_alternatif, periode, trend, sig, rate_change, family) %>%
  mutate(
    # Estimate = round(Estimate, digits = 2), 
    rate_change = round(rate_change, 2)) %>%
  rename(
    "Code Espèce" = esp_code_alternatif,
    "Periode" = periode,
    "Tendance" = trend,
    # "Coefficient" = Estimate,
    "Significativité" = sig,
    "Taux de changement" = rate_change,
    "Famille de distribution" = family
  ) %>%
  arrange(`Code Espèce`) # Ordonner par espèce, indicateur et stade

# Lignes positives et significatives
i_vert  <- which(
  df_zip_glmm_resultats$Tendance == "\U2197" &
    df_zip_glmm_resultats$Significativité %in% c("*", "**", "***")
)
# Lignes négatives et significatives
i_rouge <- which(
  df_zip_glmm_resultats$Tendance == "\U2198" &
    df_zip_glmm_resultats$Significativité %in% c("*", "**", "***")
)

tab_zip_glmm_resultats <- df_zip_glmm_resultats %>%
  flextable::flextable() %>%
  flextable::autofit() %>%
  flextable::colformat_int(j = c(1, 3), big.mark = " ") %>%
  flextable::theme_vanilla() %>%
  flextable::font(fontname = "calibri") %>%
  flextable::fontsize(size = 13) %>%
  flextable::bg(i = i_vert, bg = "darkolivegreen3") %>%   # vert clair
  flextable::bg(i = i_rouge, bg = "coral3") %>%   # rouge clair
  flextable::align(align = "center") %>%
  flextable::set_caption("Synthèse des tendances des effectifs des populations de poissons d'eau douce de BFC")

tab_zip_glmm_resultats
```


# Occurence en fonction des annes
```{r}
# preparer la table pour jointure
ope_selection <- ope_selection %>%
  dplyr::select(ope_id, pop_id, sta_id)

# Ajout des coordonnées pour jointure avec df pour future analyse autocorrelation spatiale
coord_sta_id <- point_prelevement %>%
  filter(pop_id %in% pop_serie_tempo) %>%
  rename(x = pop_coordonnees_x, y = pop_coordonnees_y) %>%
  select(pop_id, x, y) %>%
  mutate(pop_id = as.factor(pop_id))

# prepéarer la table pour jointure 
esp_ope_selection_join <- esp_ope_selection %>% 
  select(ope_id, ope_surface_calculee) %>% 
  distinct(ope_id, .keep_all = TRUE)


ope_occurence_glmm <- ope_indicateur %>%
  filter(indicateur == "occurence") %>% 
  mef_ajouter_type_protocole() %>% 
  left_join( y= ope_selection, by = c("ope_id", "pop_id", "annee")) %>% 
  inner_join(esp_ope_selection_join, by = "ope_id") %>% 
  mutate(
    valeur = as.numeric(valeur) ,
    annee = as.numeric(annee),
    ope_surface_calculee = as.numeric(ope_surface_calculee),
    pro_libelle = as.factor(pro_libelle),
    pop_id = as.factor(pop_id),
    sta_id = as.factor(sta_id),
    stade = as.factor(stade)
  ) %>%
  left_join(coord_sta_id, by = "pop_id") %>%
  mutate(x = as.numeric(x), y = as.numeric(y)) %>%
  rename(espece = esp_code_alternatif) %>% 
  filter(stade == "ind")
```

```{r}
source(file = "../R/glmm_calcul_modele.R")
source(file = "../R/glmm_application_modele.R")
```

```{r}
liste_periodes_etude <- list(c(2007, 2025)) # Liste des périodes (chaque période est un vecteur avec l'année de début et l'année de fin) 
```

### Application du modèle GLMM avec les effectifs

```{r Exécution fonction GLMM}
glmm_resultats <- glmm_application_modele(data = ope_occurence_glmm,
                                          liste_periodes = liste_periodes_etude)
```


```{r Mise en forme sortie fonction GLMM}
glmm_resultats_annee <- glmm_resultats %>% 
  filter(row_name == "annee") %>% 
  separate_wider_delim(periode, delim = "-", 
                        names = c("annee_debut", "annee_fin"), 
                        cols_remove = FALSE) %>%  # garde la colonne periode originale
  mutate(
    annee_debut = as.numeric(annee_debut),
    annee_fin   = as.numeric(annee_fin),
    nb_annees   = annee_fin - annee_debut
  ) %>% 
  select(periode, esp_code_alternatif, p_value, Estimate, sig, family, nb_annees) %>% 
  mutate(
    trend = case_when(
      Estimate > 0 ~ "\U2197",
      Estimate < 0 ~ "\U2198",
      TRUE ~ "."
    )
    # ,    # Ajouter le calcul de taux de changement dans la population
    # rate_change = (exp(Estimate * nb_annees) - 1) * 100
)
```


```{r Sélection des données résultats GLMM}
# Filtrer les données selon les critères spécifiés
glmm_resultats_format <- glmm_resultats_annee %>%
  filter((esp_code_alternatif == "ANG" & periode == "2007-2025") |
    (esp_code_alternatif %in% c(c("ABL","BAF","BLN","BRB", "BRE","BRO","CCO","CHA","CHE", "EPI", "EPT", "GAR", "GOU","HOT","LOF",   "LOR", "LOT","LPM" ,"LPP", "OBR", "PER", "ROT", "SAT", "SPI", "TAN", "TOX", "TRF","VAI","VAN","VAR")) & periode == "2007-2025"))

```

Construction d'un tableau récapitulatif des résultats du modèle GLMM :

```{r}
df_glmm_resultats <- glmm_resultats_format %>%
  select(esp_code_alternatif, periode, trend, sig, rate_change, family) %>%
  mutate(
    # rate_change = round(rate_change, 2)
    Estimate = round(Estimate, digits = 2),
    ) %>%
  rename(
    "Code Espèce" = esp_code_alternatif,
    "Periode" = periode,
    "Tendance" = trend,
    "Coefficient" = Estimate,
    "Significativité" = sig,
    # "Taux de changement" = rate_change,
    "Famille de distribution" = family
  ) %>%
  arrange(`Code Espèce`) # Ordonner par espèce, indicateur et stade

# Lignes positives et significatives
i_vert  <- which(
  df_glmm_resultats$Tendance == "\U2197" &
    df_glmm_resultats$Significativité %in% c("*", "**", "***")
)
# Lignes négatives et significatives
i_rouge <- which(
  df_glmm_resultats$Tendance == "\U2198" &
    df_glmm_resultats$Significativité %in% c("*", "**", "***")
)

tab_glmm_resultats <- df_glmm_resultats %>%
  flextable::flextable() %>%
  flextable::autofit() %>%
  flextable::colformat_int(j = c(1, 3), big.mark = " ") %>%
  flextable::theme_vanilla() %>%
  flextable::font(fontname = "calibri") %>%
  flextable::fontsize(size = 13) %>%
  flextable::bg(i = i_vert, bg = "darkolivegreen3") %>%   # vert clair
  flextable::bg(i = i_rouge, bg = "coral3") %>%   # rouge clair
  flextable::align(align = "center") %>%
  flextable::set_caption("Synthèse des tendances des Occurences des populations de poissons d'eau douce de BFC")

tab_glmm_resultats
```
